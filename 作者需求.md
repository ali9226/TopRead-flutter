# 作者创作、正文存储与审核发布需求交接

更新时间：2026-09-10。面向接手本项目的开发者和 AI。

**当前结论：正文存储和新创作工作流已经实现，相关测试及 Flutter 编译通过；但不能认定整个产品已经完成端到端验收。** 本次交接审阅新发现：管理后台旧审核入口没有统一到新工作流、读者本地缓存缺少版本标识。这两项必须优先处理。段落评论目前只有数据库基础，尚未实现完整接口和阅读端交互。

本文说明需求、实现位置和后续工作，不代表已执行部署，也不代表历史正文已全部迁移完成。本次编写文档仅核对代码和本地数据库结构，没有修改业务代码、运行正文迁移或重新执行测试。

## 1. 接手时先看这些

| 名称 | 实际目录／文件 | 用途 |
| --- | --- | --- |
| APP | `/Users/ali/work/小说/app` | Flutter 客户端 |
| BE | `/Users/ali/work/小说/后端` | Node.js 后端与数据库迁移 |
| ADMIN | `/Users/ali/work/小说/前端` | Vue 管理后台；不是 Flutter 客户端 |
| 后端规范 | [后端开发规范.md](/Users/ali/work/小说/后端/后端开发规范.md) | ES6、公共方法、中文 TODO 注释等规范 |
| 数据库关系 | [数据库表关系.md](/Users/ali/work/小说/后端/数据库表关系.md) | 项目现有表说明，结合实际库核对 |
| Flutter 规范 | [PROJECT_CONVENTIONS.md](/Users/ali/work/小说/app/PROJECT_CONVENTIONS.md) | 文件命名、代码组织、格式化与检查 |
| 正文设计说明 | [小说正文存储与版本设计.md](/Users/ali/work/小说/后端/docs/小说正文存储与版本设计.md) | 正文独立存储与迁移设计 |
| 后端工作流说明 | [creator_content/README.md](/Users/ali/work/小说/后端/utils/creator_content/README.md) | 当前新接口、事务、审核发布逻辑 |
| Flutter 编辑说明 | [work_editor/README.md](/Users/ali/work/小说/app/lib/pages/work_editor/README.md) | 编辑器与作品管理页协作 |

下文 `APP/...`、`BE/...`、`ADMIN/...` 均按上表展开。它们是不同工程，修改和检查应分别进行。

本地数据库为 MySQL，连接主机使用 `127.0.0.1:3306`，数据库 `novel`，账号 `ali`。密码从 `BE/config/data_environment.js` 或环境变量读取，本文不重复保存密码。`0.0.0.0` 是用户原先提供的地址，连接代码默认使用回环地址。

### 用户已经明确的约束

- 用户允许直接调整本地数据库结构，不需要反复征求同一项授权。
- **实际正文批量迁移脚本由用户运行，AI 不得自行启动、重跑或并发启动。** 用户曾反馈“迁移顺利”，但没有提供最终完成报告。
- 后续如需检查迁移，先做只读核对，不把“迁移程序已写好”等同于“全库已迁移完”。
- 保留当前工作区已有修改；两个工程有未提交／未跟踪文件，不要用批量恢复、清理或覆盖操作丢弃它们。特别注意 `BE/要求.txt` 等用户文件。
- 本文是交接快照。继续开发后更新“已完成、待办、测试、风险”记录，不能让文档长期停留在旧状态。

## 2. 用户最终想得到什么

过去小说正文上传到 OBS，数据库只存 URL。现在需要把正文直接存入数据库，支持每篇／每章约 1～2 万字符及更大的文本，同时让列表和目录查询保持轻量。

短篇是一份正文；长篇是一组有顺序、可独立修改的章节。作者应该能保存草稿、放弃草稿、选章提交审核、查看审核状态、按审核结果继续修改，并支持定时发布。**已发布内容的修改，在审核通过并真正发布前，其他用户必须继续看到旧版本。**

后续还要实现段落评论：段落旁展示评论数量，点击查看评论和回复。段评不能错挂到后来改写的另一个段落上。

### 已接受的交互与规则

| 场景 | 应有行为 |
| --- | --- |
| 新建作品 | 先得到稳定的作品 ID，再完善资料和正文。作品尚未公开，草稿仅作者可见 |
| 短篇保存 | 保存作品资料和短篇正文草稿，不自动发布 |
| 长篇保存 | 资料单独保存；每章独立打开、独立保存，不把整本正文随目录下载或提交 |
| 修改已发布作品 | 建立基于当前公开版本的新草稿；公开正文和标题等资料暂不被覆盖 |
| 首次发布长篇 | 需要作品资料，并选择至少一个完整章节；可一次选择多章 |
| 更新部分章节 | 只审核勾选的章节；未勾选草稿不能随同发布 |
| 正在审核 | 已提交的那份稿件锁定；其他未提交章节仍可写草稿 |
| 活动审核单 | 同一作品一次只允许一个活动批次，包含审核中和批准后等待定时公开的批次 |
| 撤回 | 仅待审核状态允许；开始审核后不允许撤回 |
| 驳回／撤回 | 保留原审核快照，复制新的可编辑草稿，不原地解锁并覆盖旧审核稿 |
| 立即发布 | 审核通过时，在同一事务中切换本次选中内容的公开版本 |
| 定时发布 | 审核通过后先等待；时间到了才切换。若审批时约定时间已经过去，按当前实现立即发布 |
| 取消定时发布 | 仅尚未实际公开的新流程批次可取消；保留新草稿，旧公开内容不变 |
| 放弃草稿 | 不删除原来已经公开的作品或章节；放弃资料草稿不顺带丢弃其他章节草稿 |
| 删除公开章节 | 先产生删除申请草稿，提交并审核通过后才从公开目录移除；不能删光全部章节 |
| 调整章节顺序 | 当前实现是相邻上移／下移，生成两章的调整草稿，须一起审核 |
| 删除整部作品 | 当前作者入口只允许删除从未公开且没有活动审核单的作品，使用逻辑删除 |
| 保存冲突 | 提示版本冲突并保留本机内容；不能自动用最后一次请求覆盖其他设备的新内容 |
| 网络失败 | 保留本机恢复副本，明确提示尚未同步；网络重试必须复用原请求标识和原内容 |

“一次只允许一个活动审核批次”“选中内容整批通过／整批驳回”“相邻章节换序”是当前落地规则。任意拖拽排序、部分章节单独通过一个批次、同章同时存在多条编辑分支，目前不是已实现能力。

## 3. 数据到底存在哪些表

### 3.1 主要表与责任

| 表 | 关键字段 | 应该在这里做什么 |
| --- | --- | --- |
| `novel_main` | `id`、`author_id`、`work_type`、`public_status`、`initial_audit_status`、`publish_status`、`remove_status`、`last_submission_id` | 稳定作品身份、归属、公开／删除状态和作品级统计；不存大正文 |
| `novel_main_language` | `id`、`novel_id`、`language_id`、`published_revision_id`、`latest_draft_revision_id`、`chapter_count`、`word_count` | 作品某个语种的公开资料；短篇公开正文从这里的发布指针查找；历史短篇 URL 在 `content_url` |
| `novel_work_revision` | `id`、`novel_language_id`、`revision_no`、`revision_status`、`lock_version`、`base_revision_id`、资料快照字段 | 作品资料修订和短篇正文所属修订；长篇资料也使用此表，但长篇各章正文不挂在这里 |
| `novel_chapter` | `id`、`novel_language_id`、`chapter_no`、`sorting`、`published_revision_id`、`remove_status` | 稳定的公开章节记录与目录顺序；历史长篇 URL 在 `content_url` |
| `novel_chapter_revision` | `id`、`chapter_uid`、`chapter_id`、`novel_language_id`、`revision_no`、`revision_status`、`lock_version`、`base_revision_id`、`change_type`、`requested_chapter_no` | 某个逻辑章节的草稿、审核稿和历史发布版本；新章尚未公开时 `chapter_id` 可为空 |
| **`novel_content_body`** | **`content LONGTEXT`**、`work_revision_id`、`chapter_revision_id`、`content_hash`、`char_count`、`byte_count`、`paragraph_count` | **完整纯文本正文实际保存处**。短篇绑定作品修订；长篇绑定章节修订 |
| `novel_content_paragraph` | `id`、`body_id`、`paragraph_no`、`start_offset`、`end_offset`、`content_hash`、`comment_count` | 某一正文版本内的段落索引，不重复存整段文本 |
| `novel_paragraph_comment` | `id`、`user_id`、`paragraph_id`、`parent_id`、`comment_content`、`remove_status` | 未来段落评论与回复；表已建，业务接口和客户端仍待开发 |
| `novel_creator_submission` | `id`、`novel_id`、`status`、`release_status`、`release_mode`、`scheduled_publish_time`、`actual_publish_time`、`workflow_version`、审核及重试字段 | 一次审核／发布批次，不是正文表 |
| `novel_creator_submission_item` | `submission_id`、`target_type`、`target_id` | 本次提交的固定修订集合；`target_type=1` 是作品修订，`2` 是章节修订 |
| `novel_creator_operation` | `author_id`、`operation`、`request_key`、`payload_hash`、`result`、`create_time` | 请求幂等记录，防止响应丢失后的重复建稿／保存／投稿；不存正文副本 |
| `novel_content_migration_log` | `content_type`、`content_id`、`novel_id`、`source_hash`、`status`、`body_id`、`last_error` | 迁移结果、失败与重试依据；不可把它当成作品审核状态 |
| `novel_main_category_rel` | `novel_id`、`category_id` 等 | 正式公开的分类关系；资料草稿的分类在修订快照内，发布时才替换 |

### 3.2 正确的正文关联

```text
短篇公开正文：
novel_main_language.published_revision_id
  → novel_work_revision.id
  → novel_content_body.work_revision_id
  → novel_content_body.content

长篇公开正文：
novel_chapter.published_revision_id
  → novel_chapter_revision.id
  → novel_content_body.chapter_revision_id
  → novel_content_body.content

段落与评论：
novel_content_body.id
  → novel_content_paragraph.body_id
  → novel_paragraph_comment.paragraph_id
```

必须记住：

1. `novel_id`、`novel_language_id`、`chapter_id`、`revision_id`、`body_id` 是不同身份，不可混用。公开正文接口短篇传语种 ID，长篇传章节 ID；作者编辑接口传修订 ID。
2. `novel_content_body.work_revision_id` 和 `chapter_revision_id` **必须且只能有一个非空**，并各有唯一约束。一份修订最多一份正文。
3. 草稿正文允许更新；已提交审核或发布的正文不可原地更新。放弃后重建草稿通常有新的修订 ID 和正文 ID。
4. 多条修订都可能处于“曾发布”状态 3；**只有主表的 `published_revision_id` 决定当前公开版本**。禁止用最大 ID、最新时间或单独的状态 3 查“当前正文”。
5. `base_revision_id` 表示这次改写以哪份公开版本为基础；它不是草稿乐观锁。`lock_version` 用于阻止同一草稿被多设备相互覆盖。
6. `draft_guard`、`pending_guard` 是数据库生成字段及唯一约束的一部分，不应在克隆时手工赋值；业务层还会禁止同章已有待审稿时另开编辑稿。
7. 历史内联字段 `novel_work_revision.temp_chapter_content`、`novel_chapter_revision.content` 和 `content_url` 暂时保留作兼容，不能未经全量核查就删除。
8. 标题、目录和数量查询不要 JOIN 返回 `LONGTEXT`。新目录只返回元数据，打开单章时才加载正文。分表有帮助，但无法代替分页、索引和发布事务性能优化。

### 3.3 段落的精确定义

- 当前切段器按非空文本行建立段落索引，空白行不单独建段；完整正文仍保留换行、缩进与空格。
- `start_offset`／`end_offset` 为 **UTF-16** 偏移，结束位置不包含在片段中。JavaScript `slice` 与 Dart 字符串切片必须按同一规则使用。
- `char_count` 是 Unicode 码点总数，`byte_count` 是 UTF-8 字节数；它们与 UTF-16 偏移、阅读字数不是同一个数。
- `novel_content_paragraph.id` 仅在这一正文版本内有效。新正文中的相同段落不会自动复用旧段落 ID。
- `comment_count` 的表定义是未删除评论总数，包含回复。未来创建、删除、恢复评论时须在同一事务维护，不能直接把小说级评论数当成段评数。
- 父评论和回复必须属于同一段落，数据库已有复合外键约束。

## 4. 状态不能混用

### 4.1 作品与修订

| 字段 | 值与含义 |
| --- | --- |
| `novel_main.work_type` | 1 长篇；2 短篇 |
| `novel_main.public_status` | 1 未公开；2 已公开；3 作者下架；4 平台下架 |
| `novel_main.initial_audit_status` | 1 初始草稿；2 首次待审核；3 首次通过；4 首次驳回。不能单靠它判断已发布作品的新投稿 |
| `novel_main.publish_status` | 1 长篇连载；2 长篇完结；3 下架／未公开兼容值；4 短篇。它不是审核状态 |
| 两种修订表的 `revision_status` | 1 可编辑草稿；2 已锁定的待审／待发布快照；3 曾发布版本；4 驳回快照；5 已放弃／撤回快照 |
| 章节修订 `change_type` | 1 新增；2 修改；3 删除申请 |
| 章节修订 `requested_chapter_no` | 非空是显式申请的位置；为空表示本次不要求换序，不能把旧草稿里的 `chapter_no` 当成当前排序指令 |

### 4.2 新审核流程（`workflow_version=2`）

| 字段 | 值与含义 |
| --- | --- |
| `novel_creator_submission.status` | 1 待审核；2 审核中；3 通过；4 驳回；5 撤回 |
| `release_status` | 1 等待审核；2 已批准等待公开；3 已实际公开；4 已取消；5 发布失败待重试 |
| `release_mode` | 1 立即；2 定时 |

- 定时等待：审核状态 3，发布状态 2，修订仍为 2，公开指针未变。
- 实际发布：审核状态 3，发布状态 3，本批次修订变为 3，公开指针切换。
- 取消定时：审核状态保留 3，发布状态变 4，旧投稿快照保留，另建草稿；不能仅凭 `status=3` 显示“已公开”。
- 数据库约束要求立即模式不带定时时间，定时模式必须有时间。客户端提交 UTC 时间，显示时转换成本地时间。
- 历史 `workflow_version=1` 的代码曾使用不同发布状态语义，且可能提前切换了正文指针。**不要直接批量把历史单的 workflow_version 改成 2。**

## 5. 当前代码在哪里

### 5.1 Flutter 创作者端

| 路径 | 责任／关键入口 |
| --- | --- |
| `APP/lib/pages/author_center/author_view.dart` | 创作者中心列表、新建作品、继续写作、进入作品管理页 |
| `APP/lib/pages/author_center/models/creator_backend_models.dart` | 作品、审核、草稿数量等返回数据模型 |
| `APP/lib/pages/author_center/widgets/backend_work_card.dart` | 公开状态、草稿、审核和定时发布标记 |
| `APP/lib/pages/work_editor/workspace/create_page.dart` | 先选篇幅、语种、标题，创建稳定作品记录 |
| `APP/lib/pages/work_editor/workspace/index.dart` | `CreatorWorkspacePage`；资料、章节、审核记录、选章投稿与撤回等交互 |
| `APP/lib/pages/work_editor/workspace/logic.dart` | `WorkspaceController`；概览、目录／历史分页、选择状态 |
| `APP/lib/pages/work_editor/index.dart` | `CreatorWorkEditorPage`；资料／短篇正文编辑、自动保存和本机备份 |
| `APP/lib/pages/work_editor/draft_persistence.dart` | `CreatorDraftPersistence`；作品保存会话、锁版本和不确定网络结果的原请求重试 |
| `APP/lib/pages/work_editor/work_recovery.dart` | 资料／短篇草稿恢复；比较云端和本机后再选择 |
| `APP/lib/pages/work_editor/backend_draft_loader.dart` | 后端草稿数据转换，检查正文是否成功加载 |
| `APP/lib/pages/work_editor/single_chapter/index.dart` | `SingleChapterPage`；单章编辑、只读预览、保存状态与冲突比较 |
| `APP/lib/pages/work_editor/single_chapter/logic.dart` | `ChapterDraftController`；3 秒防抖保存、请求串行、本机恢复副本 |
| `APP/lib/api/creator_workspace.dart` | 新工作台的统一请求与异常边界 |
| `APP/lib/api/creator_work.dart` | 原作品 API，仍被保存会话复用 |
| `APP/lib/api/post_request.dart`、`results_type.dart` | 公共加密请求；`serverRejected` 区分服务端明确拒绝与网络结果不明 |
| `APP/lib/util/storage_util/index.dart` | GetStorage 本机存储封装 |
| `APP/assets/i18n/{zh,en,fr,sw}.json` | 新文案在 `creator_workspace` 命名空间，四个语种均已补充 |

编辑器参数的实际含义：

- `metadataOnly:true`：长篇只编辑资料和分类，保存时 **省略 `chapters` 字段**。不能传空数组代替省略，旧整本保存逻辑会把它理解成删除章节。
- `saveOnly:true`：不在旧编辑器内提交审核，保存后回作品管理页决定投稿范围。长篇为两步，短篇为三步。
- `restorePending:true`：刚恢复了本机未同步内容，需要继续保存。
- 作品编辑当前约每 500 毫秒尝试本机备份、每 3 秒尝试云端保存；单章是输入触发本机备份、3 秒防抖云端保存。后台挂起和跨修订恢复仍有待补强，见风险清单。

### 5.2 后端

| 路径 | 责任 |
| --- | --- |
| `BE/routers/creator_work/index.route.js` 及同目录文件 | 作品创建、概览、资料／短篇草稿、投稿、历史、撤回、取消与删除 |
| `BE/routers/creator_chapter/index.route.js` 及同目录文件 | 单章草稿、目录、删除申请、换序、单章／批量投稿 |
| `BE/utils/creator_auth.js` | 创作者身份及作品归属校验 |
| `BE/utils/creator_content/handler.js`、`lock_work.js`、`operation.js` | 写接口的权限、锁顺序、事务和幂等入口 |
| `BE/utils/creator_content/store_body.js` | 校验草稿状态后写独立正文，重建当前草稿的段落索引 |
| `BE/utils/creator_content/revision_content.js` | 加载指定修订正文；兼容旧内联字段和可信 OBS 地址 |
| `BE/utils/creator_content/chapter_draft.js`、`clone_revision.js` | 创建单章工作副本，或从固定审核稿复制新草稿 |
| `BE/utils/creator_content/submit_bundle.js` | 校验选中范围、授权和发布时间，将选中修订冻结为一张审核单 |
| `BE/utils/creator_content/build_directory.js` | 提交与发布共用的目录校验，防止空目录和换序冲突 |
| `BE/utils/creator_content/publish_bundle.js` | 真正的公开切换；核对基线、更新目录与指针，整批事务提交 |
| `BE/utils/creator_content/resolve_review.js` | 驳回、撤回、取消；保留旧审核快照并另建可编辑稿 |
| `BE/utils/creator_content/config.js` | 正文上限、修订状态、发布状态、活动审核条件 |
| `BE/utils/creator_workflow.js` | 通用事务、分页、字数及旧整本章节同步兼容逻辑；旧同步是并发风险关注点 |
| `BE/utils/creator_editor_snapshot.js` | 为旧编辑器组合公开章节与作者草稿 |
| `BE/routers/admin_submission/` | 新审核入口：详情、开始审核、通过、驳回 |
| `BE/environment/定时发布小说/server.js` | 定时服务入口，每分钟检查；不是主接口服务自动启动的任务 |
| `BE/environment/定时发布小说/utils/task.js` | `run_due_publications`；每轮最多处理 50 个到期单，失败约 5 分钟后重试 |
| `BE/routers/novel_content/get_content_source_url.js` | 按公开指针查正文，兼容函数名虽然含 URL，实际也返回数据库正文 |
| `BE/routers/novel_content/get_content_text.js` | 优先数据库正文，未迁移时才使用旧 URL 回源及 Redis 缓存 |
| `BE/routers/novel_content/read.js`、`encrypt_content.js` | 读者正文接口、既有混合加密输出 |
| `BE/routers/admin_work/chapter_content.js`、`novel_detail.js` | 管理后台正式内容读取兼容 |

### 5.3 管理后台与读者端（下一步必须一起查）

| 路径 | 关注点 |
| --- | --- |
| `ADMIN/src/api/novel.js` | 有新审核通过／驳回封装，但缺少 `start_review` 封装；不能据此认为页面已经接入新审核链路 |
| `ADMIN/src/views/admin/novel/index.js` | 实际审核动作仍调用 `API.novel_action`，详见 B01 |
| `ADMIN/src/views/admin/novel/components/NovelDetailDialog.vue` | 按作品首次审核状态显示审核按钮，不能完整管理已发布作品的新更新审核 |
| `BE/routers/admin_work/novel_action.js` | 旧审核实现，与新固定快照／发布状态机制不兼容，优先统一 |
| `APP/lib/pages/read/logic.dart`、`utils/chapter_cache.dart` | 长篇读者缓存，当前以章节 ID 为键，详见 B02 |
| `APP/lib/pages/short_story_read/logic.dart`、`utils/short_story_content_cache.dart` | 短篇内存／磁盘缓存，当前以语种 ID 等稳定身份为键 |
| `APP/lib/api/get_novel_content.dart` | 读者正文请求与解密入口 |
| `APP/lib/pages/read/index.dart`、`APP/lib/pages/short_story_read/index.dart` | 后续段落计数标记、点击展开评论等阅读交互入口 |

## 6. 接口约定与一次完整操作

所有下列业务参数都应通过现有请求封装发送，不要另造明文网络协议。OBS 历史正文按纯 UTF-8 文本下载；“下载正文”和“客户端接口加解密”是两件不同的事。

公共 `postRequest` 会写入当前界面语种的 `language_id`。**创作语种使用 `work_language_id`，作品语种记录使用 `novel_language_id`，不要与界面语种混淆。**

### 6.1 接口速查

| 接口 | 必要／主要参数 | 返回或效果 |
| --- | --- | --- |
| `creator_work/create_draft` | `work_type`、`work_language_id`、`title`、建议 `request_key` | 作品 ID、语种 ID、作品修订 ID、锁版本 |
| `creator_work/workspace` | `novel_id`，可选 `novel_language_id` | 资料摘要、当前草稿、活动单、权限、语种列表；无长篇正文 |
| `creator_work/begin_edit` | `novel_id`，可选 `novel_language_id` | 创建／复用资料或短篇工作副本 |
| `creator_work/get_info` | `novel_id`、`novel_language_id`、`include_chapters:false` | 当前草稿资料及短篇正文；长篇新入口不要省略 false |
| `creator_work/save_draft` | `novel_id`、`revision_id`、`lock_version`；资料字段、短篇 `short_content` | 成功返回新锁版本；缺少锁版本会拒绝 |
| `creator_chapter/directory` | `novel_id`，可选语种、`page`、`page_size`、`state`、`keyword` | 元数据分页；状态筛选 `all/draft/pending`，不返回正文 |
| `creator_chapter/begin_edit` | `novel_id`；编辑旧章传 `chapter_id`，新章传 `novel_language_id` | 单章草稿修订 ID 与锁版本 |
| `creator_chapter/get_info` | `revision_id` | 作者有权查看的这一份修订正文 |
| `creator_chapter/save_draft` | `revision_id`、`lock_version`、`title`、`content`、建议 `request_key` | 只保存此章，返回新锁版本 |
| `creator_chapter/delete_draft` | `revision_id` | 放弃单章草稿，不删除原公开章 |
| `creator_chapter/request_delete` | `novel_id`、`chapter_id` | 生成删除申请草稿 |
| `creator_chapter/move` | `novel_id`、`chapter_id`、`direction:up/down` | 返回两个须一起提交的修订 ID |
| `creator_work/submit` | `novel_id`、可选作品 `revision_id`、明确的 `chapter_revision_ids`、`rights_confirmed:true`、`release_mode`、定时时间 | 原子冻结选中稿件并生成审核单 |
| `creator_chapter/submit`、`batch_submit` | 复用选章投稿参数 | 使用同一提交服务 |
| `creator_work/submissions` | `novel_id`、分页参数 | 审核历史元数据 |
| `creator_work/submission_info` | `submission_id`，可选 `item_id` | 不传 item 只看目录；传 item 才读固定审核正文 |
| `creator_work/withdraw_submission` | `submission_id` | 仅状态 1 可撤回，返回新建草稿 ID |
| `creator_work/cancel_release` | `submission_id` | 取消尚未发布的新流程定时批次并复制草稿 |
| `creator_work/discard_draft` | `novel_id`、`revision_id` | 放弃资料／短篇草稿 |
| `creator_work/delete` | `novel_id` | 仅从未公开且无活动审核单的作品可逻辑删除 |
| `admin_submission/start_review` | 审核单 `id` | 状态 1 → 2；此后作者不能撤回 |
| `admin_submission/approve` | 审核单 `id`，可选 `review_note` | 使用新发布服务；定时未到时不切指针 |
| `admin_submission/reject` | 审核单 `id`、`review_note` | 保留快照并另建可编辑草稿 |
| `novel_content/read` | 既有正文协议：`content_type`、`content_id` 等，由读者封装负责 | 返回既有加密正文；它不是未来段落元数据 API |

目录每页后端最多 100 条；Flutter 默认每次加载 50 条。一次显式选章投稿最多 100 个不重复修订。正文上限目前为 8 MiB UTF-8 字节，不是 8 百万汉字。

### 6.2 典型长篇流程

1. 创建作品，拿到作品／语种／资料草稿 ID。
2. 保存资料，**不传 `chapters`**。
3. 分别创建 A、B、C 三章，分别按各自 `lock_version` 保存正文。
4. 首次投稿选择资料草稿及 A、B 两章；C 不选。
5. A、B 和资料锁定，C 继续可编辑；读者尚看不到首次投稿。
6. 通过新审核接口批准：立即模式公开 A、B；定时模式等实际到时再公开。C 始终保留草稿。
7. 修改已公开 A，基于 A 的公开修订创建新草稿；提交后，读者继续看旧 A。
8. 驳回或撤回时得到新的草稿 ID；重新加载管理页，不继续向旧审核修订保存。
9. A 的更新实际发布后，章节 ID 保持不变、发布修订 ID 改变。因此读者缓存必须识别版本变化，不能只看章节 ID。

短篇使用同样的版本规则，只是正文属于作品修订，不需要章节数组。

### 6.3 幂等与并发的边界

- 同一次网络重试沿用同一个 `request_key`、同一份业务内容、同一个原锁版本；成功后采用服务端返回的新锁版本。
- 响应丢失时不能直接把稍后输入塞进原 request_key；先确认原请求的结果，再生成新请求保存后来输入。
- 服务端明确拒绝与网络结果不明已区分；版本冲突不自动忽略。
- `operation.js` 忽略传输层 `request_time`、`timezone`、`device_environment`，其余参数参与摘要。
- 幂等参数目前不是所有旧入口强制要求的，不能假设所有历史客户端都具备新端的重试保障。
- 同作品写操作通常先锁作品，再锁相关修订／审核单；后续改动沿用该顺序。旧入口是否都遵守仍须核查。

## 7. 已完成、未完成与验证范围

### 7.1 已实现并有相关测试覆盖

- [x] 独立正文、段落、段评基础、迁移日志，以及工作流补充结构。
- [x] 正文迁移程序、URL 失败分类、重试和逻辑删除规则。
- [x] 新创作者入口、逐章正文保存、长篇资料保存与章节分离。
- [x] 选章投稿、固定审核快照、公开版本隔离、撤回／驳回后复制草稿。
- [x] 新审核接口、定时发布处理服务、取消定时更新、章节删除和相邻换序。
- [x] 自动保存、本机副本、网络重试及基本多设备锁冲突处理。
- [x] 多语种草稿定位、只有章节草稿时的作品统计、状态标记和四语种文案。
- [x] 用户反馈的 `metadataOnly` 构造参数编译错误已修复。

这里的“已实现”描述新链路，不代表所有旧入口和读者缓存已经同步改好。

### 7.2 当前明确未完成／未确认

| 工作 | 现状 | 接下来在哪里做 |
| --- | --- | --- |
| 管理后台统一新审核链路 | **未完成，存在旧入口冲突，见 B01** | ADMIN 小说审核页面、API 封装；BE 旧审核兼容入口 |
| 读者版本缓存更新 | **未完成，见 B02** | 长／短篇 reader logic、内存缓存、磁盘缓存、目录／详情版本元数据 |
| 全库正文迁移完成核验 | 未确认，用户最后反馈仍在迁移 | 只读检查迁移日志与当前发布指针对应正文；由用户决定何时重跑迁移 |
| 真实 HTTP 与部署环境联调 | 未完成完整验收记录 | 管理员／作者／读者不同账号、真实加密请求、实际 MySQL 外键及定时服务 |
| 段评接口与阅读交互 | **仅建表，未开发完成** | `novel_content_paragraph`、`novel_paragraph_comment`，BE 新接口，APP 两类阅读页 |
| 跨正文版本的段评展示政策 | 未定，当前不会自动搬迁 | 先确定保留旧版还是映射新段落，再设计映射与校验 |
| 本机草稿恢复边界 | 同一修订基本恢复已实现，跨修订／损坏副本／系统挂起待补强 | 两类编辑器、work_recovery、StorageUtil |
| 多语种创作切换 UI | 后端可指定语种，新管理页主要按原始语种工作 | workspace 控制器与页面；不能仅凭返回 languages 就视为已有切换能力 |
| 大目录发布性能 | 正文分表与分页已做，发布仍有全目录更新成本 | publish_bundle、build_directory、SQL 索引与压力测试 |
| 大规模草稿／历史清理策略 | 未实现完整保留期与清理任务 | operation、修订／正文历史、段评外键依赖 |

### 7.3 上一轮验证记录（本次文档任务未重跑）

- Flutter：`test/pages/work_editor` 和 `test/pages/author_center` 共 **43 项测试通过**。
- 后端：指定测试集合 **17 项通过，1 项实际迁移集成测试跳过**；包含两个独立临时 MySQL 库的工作流集成用例。
- `flutter build bundle --debug --no-pub` 成功。这是 Flutter 构建验证，不等同于 iPhone 模拟器每条业务路径已人工验收或原生安装包签名完成。
- Dart 静态检查未发现编译错误；仍有旧代码未使用导入／局部变量等 warning 和 lint 提示。
- 本地 `novel` 库的 004／005 相关结构已应用；本次又只读确认了关键字段和状态注释。
- 工作流测试使用 `CREATE TABLE ... LIKE novel...` 创建临时表，**不会完整复制源表外键约束**，也不等于经过生产请求中间件、管理员鉴权与真实连接池全链路测试。

主要测试文件：

| 路径 | 覆盖内容 |
| --- | --- |
| `BE/test/creator_workspace.test.js` | 逐章草稿、选章首次发布、开始审核禁止撤回、定时到期、克隆快照、越权保存、删除／换序、资料不动章节、大段 Unicode 正文 |
| `BE/test/creator_revision_flow.test.js` | 原编辑链路的兼容回归、公开版本隔离、驳回／撤回后新草稿 |
| `BE/test/novel_content_body_read.test.js` | 按公开指针读取正文、兼容回源、审核内容读取 |
| `BE/test/novel_content_migration.test.js` | UTF-8、段落切片、404／410 判定、网络失败与错误正文处理；实际迁移集成测试默认跳过 |
| `APP/test/pages/work_editor/chapter_draft_controller_test.dart` | 输入期间保存、不确定响应重试、冲突阻止覆盖、本机写失败 |
| `APP/test/pages/work_editor/draft_persistence_test.dart` | 作品保存会话、请求顺序、重试与锁版本 |
| `APP/test/pages/work_editor/basic_keyboard_layout_test.dart` | 编辑步骤、metadataOnly／saveOnly、键盘布局 |
| `APP/test/pages/author_center/creator_tab_state_test.dart` | 草稿模型与分页状态 |

## 8. 已知缺口与可能的 BUG：按优先级接手

分类说明：**代码确认**表示当前代码可以直接确认该行为或不兼容；**待复现**表示触发路径有依据，但本次没有做运行时复现；**产品待定**表示需要明确规则。以下问题没有在本次文档任务中顺带修改。

### B01 · P0 · 管理后台旧审核入口绕过新工作流【代码确认】

- 位置：`ADMIN/src/views/admin/novel/index.js` 调用 `API.novel_action`，经 `ADMIN/src/api/novel.js` 到 `BE/routers/admin_work/novel_action.js`。
- 旧批准逻辑直接修改审核单与作品状态，使用旧的 `release_status` 值，不调用 `publish_bundle`；新正文修订和发布指针可能没有被切换。旧驳回直接把状态 2 改回 1，不克隆快照，后来修改会覆盖原审核记录。
- 旧定时批准可能留下 `workflow_version=2`、`status=3`、`release_status=1` 的组合，新定时任务不会按新流程等待单处理它。
- 页面审核按钮还依赖 `novel.initial_audit_status===2`，已发布作品的新更新可能无法在该入口正常审核。
- 复现：作者从新管理页投稿；管理员从现有小说列表详情操作通过／驳回；核对提交项修订状态、正文指针及审核快照是否符合第 4 节。
- 修复：页面按审核单 ID 接入 `admin_submission/get_info/start_review/approve/reject`；旧 `novel_action` 必须复用新服务或明确拒绝新工作流，不能保留两套更新规则。保留管理员权限与活动日志能力。
- 验收：通过现有管理页面实际完成首次发布、更新、驳回、定时审核；不能只直接调用新路由函数测试。

### B02 · P1 · 发布更新后，读者可能继续看到旧正文【代码确认；实际出现条件待联调】

- 位置：`APP/lib/pages/read/logic.dart` 与 `utils/chapter_cache.dart`；短篇 `logic.dart` 与 `utils/short_story_content_cache.dart`。
- 长篇磁盘缓存按 `chapter_id`，短篇正文按 `novel_language_id`；磁盘 TTL 为 7 天，内存缓存也未按正文修订区分。缓存命中时可直接返回，不必请求最新正文。
- 更新审核通过后这些稳定 ID 不变，变化的是 `published_revision_id`。因此“后端已经返回新版”不能证明用户设备会请求新版。
- 复现：设备先阅读旧版形成缓存；作者更新并通过审核；回到同一作品／章节正常阅读，不强制清缓存；比较正文。
- 修复：目录／详情和读者模型携带当前公开修订 ID 或正文 hash；内存、磁盘和进行中的请求缓存使用“稳定 ID + 公开版本”。同时规定在线可读性校验、离线缓存、下架后的展示策略。
- 验收：旧版→新稿待审仍旧版→真正发布自动识别新版；长篇、短篇、内存命中和重启后磁盘命中分别测试。不要只清 Redis。

### B03 · P1 · 旧整本章节保存可能绕过单章多设备锁【代码确认风险路径，待并发复现】

- 位置：`BE/routers/creator_work/save_draft.js` → `BE/utils/creator_workflow.js::sync_chapters`。
- 旧 `chapters` 数组只校验作品资料修订的锁；同步已有章节草稿时并未要求客户端提供该章的旧 `lock_version`，而是读当前值后更新。另一设备刚保存的单章内容可能被旧整本快照覆盖。
- 已有章节待审时旧同步会拒绝，但这不能保护“仅有未提交章节草稿”的并发编辑。
- 修复：优先收紧旧整本写入口；或让每章提供版本并逐章校验，冲突则整批回滚。仅靠新 Flutter 不再发送 chapters，不能消除旧客户端的风险。
- 验收：A 打开旧整本稿，B 单章保存新版，再由 A 保存；B 的内容不能被静默覆盖。

### B04 · P1 · 修订换 ID 后，本机未同步副本可能无入口恢复【代码确认存储方式，待双设备复现】

- 位置：`single_chapter/index.dart`、`work_recovery.dart`、`BE/utils/creator_content/clone_revision.js`。
- 本机 key 包含账号和修订 ID；撤回／驳回会生成新修订 ID。单章只在修订可编辑时提示恢复，打开锁定修订时不会展示本机副本。
- 复现：A 有离线未同步内容，B 提交同一修订；A 退出后再打开，或 B 撤回生成新修订后 A 再进入，检查旧 key 下的文字是否可找回。
- 修复：增加按作品＋语种＋逻辑章节检索的恢复索引／恢复中心；允许锁定或已废弃稿只读查看、复制本机副本，再明确选择合并至新草稿。不能自动覆盖审核稿。
- 验收：旧修订本机内容不丢、可见、可导出；跨账号严格隔离。

### B05 · P1 · 定时发布可能重新公开已被平台下架的作品【代码确认风险路径，业务政策待定】

- 位置：`BE/utils/creator_content/lock_work.js`、`publish_bundle.js`、定时 `task.js`；下架入口 `BE/routers/admin_work/update_status.js`。
- 发布只要求作品未删除，最终直接置 `public_status=2`；平台下架状态 4 没有在该发布路径中被单独拦截。
- 复现：批准未来发布单→平台下架作品→到期处理，观察是否被重新公开。
- 修复：明确下架优先级，通常应暂停／取消待发布而不是自动恢复。下架操作与实际发布采用统一锁顺序，并补审核、下架、定时任务并发测试。

### B06 · P1 · 真实鉴权、外键与连接池链路覆盖不足【验证缺口】

- 测试直接构造 `ctx` 并使用临时库；新管理员路由依赖项目既有鉴权配置，不能凭函数调用测试宣称管理员权限已验收。
- 临时表的 `CREATE TABLE LIKE` 不完整复制外键，生产连接池的提交／回滚与测试上下文也不是完全相同的实现路径。
- 下一步：在专用测试环境使用真实加密 HTTP 请求，覆盖未登录、非作者、跨作者、作者访问管理员接口、非法修订归属、SQL 中途失败、提交失败、重复批准、批准与撤回并发。
- 验收：权限拒绝、事务原子性、连接归还及所有外键在真实配置下有效。

### B07 · P2 · 单章正文更新也会重写整个公开目录【代码确认性能问题】

- 位置：`publish_bundle.js`。每次有章节更新都会读取整个语种目录，把全部公开章移到临时序号，再逐条恢复最终序号并重算统计。
- 风险：数千／数万章节作品只更新一章，也产生大量 SQL 和长事务；正文分表无法消除这个成本。
- 修复：区分“仅正文／标题更新”和“新增／删除／换序”；前者只更新目标章及必要统计。目录变动再安全调整受影响区间。先记录查询数、事务时长和锁等待再优化。
- 验收：大目录单章更新的写入规模不随全书章节数线性增长，顺序唯一约束仍成立。

### B08 · P2 · 本机副本损坏或系统挂起时的恢复不足【待复现】

- `work_recovery.dart` 直接解码本机 JSON，没有隔离损坏数据的恢复入口；异常会妨碍正常进入编辑页。
- 当前自动保存依赖定时器／输入回调，没有完整的生命周期补存机制。系统快速挂起或杀进程时，最后一小段输入仍可能来不及落盘。
- 修复：损坏副本隔离、可复制原始内容、允许继续打开云端稿；增加应用后台／失焦补存及可测的本机写入结果，避免承诺不存在的恢复副本。
- 验收：模拟损坏 JSON、存储失败、输入后立刻挂起、进程退出后重开，记录可恢复范围。

### B09 · P2 · 历史定时单显示与取消规则不统一【代码确认】

- 活动条件会包含历史 `workflow_version=1/release_status=1` 的已批准单，但 `workspace` 摘要没有返回 workflow_version。
- Flutter `_reviewState` 对 status=3/release_status=1 会落到“已发布”，而取消发布入口只支持相应的新状态／新快照处理，可能显示和按钮不一致。
- 修复：返回工作流版本，明确历史状态映射和可用操作。无法还原旧快照的记录要单独处理，不能直接改标记冒充已兼容。

### B10 · P2 · 元数据校验与客户端预检不完全一致【代码确认校验不对称，待复现范围】

- `work.save_draft` 允许传部分 `word_count`、分类 JSON、发布时间和语种字段；新投稿只校验其中一部分。旧编辑器的分类必填检查与管理页直接提交路径也不完全一致。
- 短篇提交时会检查正文并写正文表，但没有统一在最终发布前从正文重算所有可被客户端传入的统计字段。
- 修复：统一服务端资料必填规则、语种有效性、分类归属、时间和长度限制；正文统计由服务端决定，不信任客户端覆盖。允许不完整草稿，但提交时严格校验。
- 验收：直接构造请求也不能发布空必填资料、错误语种、错误分类或伪造字数；错误消息可供界面定位。

### B11 · P2 · 只读正文的公开条件仍依赖兼容字段【代码确认依赖，待状态一致性验证】

- `get_content_source_url.js` 的公开读取 SQL 检查 `publish_status` 和删除状态，没有独立要求 `public_status=2`。
- 正常下架接口同时更新两种状态，所以不能据此断言当前已发生泄露；但旧入口或异常数据若只改一个字段，就可能出现判断不一致。
- 修复：核对存量状态组合、旧客户端与下架入口，统一公开可读判断并测试状态不一致的拒绝行为，不能直接假定历史数据全部规范。

### B12 · P2 · 新章发布后的修订章节号可能仍为 0【代码确认】

- 新章草稿 `chapter_no=0`；发布时给 `novel_chapter` 写正式序号、回填修订的 `chapter_id`，没有回填同一修订的 `chapter_no`。
- 公开目录主要读主表，因此不等于读者目录必然错误；但审核历史／旧章节列表按修订号排序时，可能显示 0 或错误顺序。
- 修复前先定义审核快照要展示“提交时位置”还是“最终发布位置”；可分别保留，不要为了显示而破坏固定审核含义。

### B13 · P2 · 幂等、历史正文与本机副本没有完整保留期方案【代码确认能力缺失】

- `novel_creator_operation` 持续积累自动保存结果；历史修订／正文和本机恢复副本也会增长。
- 不能简单删除正文历史：审核记录、发布基线、迁移日志和段评都有依赖。删除旧幂等记录也必须保证超出窗口的重试不会重新创建业务记录。
- 下一步先制定按操作类型和数据用途的保留期，评估索引、容量与清理审计，再实现有界清理。

## 9. 下一步开发顺序与完成标准

不要先继续堆叠新功能。建议按下面的任务顺序推进，每项完成后补回归测试并更新本文状态。

| 顺序 | 工作 | 改动重点 | 完成标准 |
| --- | --- | --- | --- |
| 1 | 统一所有后台审核入口（B01） | ADMIN 页面／API、旧 novel_action 与新审核服务 | 从真实管理页面能审核新稿；没有旁路改变快照、指针或状态 |
| 2 | 读者缓存按公开版本更新（B02） | 两类读者缓存、目录／详情版本字段、正文返回契约 | 已发布更新能被正常阅读路径识别；待审期间仍看到旧版 |
| 3 | 收紧旧章节写入口与下架发布规则（B03、B05、B11） | 旧 sync_chapters、公开状态判断、事务锁 | 多设备不丢稿，下架不会被定时任务意外恢复 |
| 4 | 补真实请求与数据库约束测试（B06） | 测试环境、真实鉴权／外键／连接池 | 作者、管理员、读者完整链路通过，失败全部回滚 |
| 5 | 完成本机恢复闭环（B04、B08） | 两类编辑器、跨修订恢复索引 | 被锁、被驳回、换 ID、损坏存储后都有明确恢复路径 |
| 6 | 核查迁移进度与部署 | 只读数据核验、进程版本、任务日志 | 有数量、失败分类与抽样结果；确认实际启用新服务 |
| 7 | 开发段落评论第一版 | 新段评接口与两类阅读页 | 段落数量、点击、分页、回复、删除、权限和计数一致 |
| 8 | 优化大目录与长期数据管理（B07、B09、B10、B12、B13） | 发布查询、兼容状态、统计校验、保留期 | 有性能指标和长期维护规则，不靠全表或全书扫描支撑常见操作 |

其中语种切换、任意排序、跨版本段评映射属于扩展需求；不要未经产品规则确认就把它们当成已完成或承诺的行为。

### 段落评论第一版的具体任务（尚未实现的建议，不是现有 API）

1. 定义“读取当前公开正文版本＋段落索引＋评论数”的 API。返回稳定作品／章节身份、公开修订 ID、body_id、paragraph_id、UTF-16 偏移及计数，不能暴露作者草稿。
2. 定义按 paragraph_id 分页读评论、写评论、回复、逻辑删除的 API。新路由名称需按现有项目规范建立，不能把本文当成这些接口已经存在。
3. 写入时确认段落属于用户有权阅读的版本；同段回复；评论长度、登录、频率和管理权限校验；评论与计数变动同一事务提交。
4. Flutter 长篇／短篇阅读页按真实段落索引渲染计数标记，点击展示评论列表和回复入口；保留现有阅读排版、滚动进度、缓存和加密机制。
5. 第一版建议评论绑定具体正文快照，不自动搬迁。更新后旧评论是否仍可查、如何提示旧版，必须形成明确产品规则。
6. 如果要把未改动段落的评论延续到新版，另行设计旧段落→新段落映射。仅靠文本 hash 不够：相同段落重复出现、位置变化、拆分／合并会造成歧义。
7. 用中文、emoji、CRLF、缩进、空行、重复段落、插入／删除段落和改写发布测试锚点。必须保证点开的评论确实属于用户当前看到的段落。

## 10. 正文迁移与部署交接

### 10.1 迁移文件与规则

入口：`BE/test/scribblehub爬虫/同步记录章节内容.js`。实现分解在 `BE/database/content_migration/`；命令说明在 `help.js`，下载判断在 `download_content.js`，单来源迁移在 `migrate_source.js`。

- 短篇来源 ID 为 `novel_main_language.id`；长篇来源 ID 为 `novel_chapter.id`。
- 已有数据库正文优先，不用旧 OBS URL 覆盖更新的内容。
- 下载 UTF-8 原文，保留空白；只允许配置的可信 HTTPS CDN。默认域名与上限见迁移 config／help。
- 多次重试全部确认 404／410，才逻辑删除整本小说。403、超时、429、5xx、空白、错误页面和乱码只记录失败，不能当成“小说不存在”。
- 失败结果、已迁移正文、来源变化有各自处理；写入前重新锁定并核对来源，防止覆盖迁移期间的新内容。
- 迁移进度、失败数量和真实完成时间目前没有最终确认。查看 `novel_content_migration_log` 和当前公开指针对应正文来核验。

**不要把本节当作执行授权。实际批量迁移继续由用户运行。** 不要为“看看效果”运行无参数入口；它会写数据库并可能逻辑删除作品。

### 10.2 数据库迁移文件

- `BE/database/migrations/001_creator_tables.sql`、`002_creator_draft_fields.sql`、`003_creator_chapter_content.sql` 是历史创作结构来源；其中定义不一定等同于当前实际数据库，不能盲目全量重放。
- `004_novel_content_storage.js` 新增正文、段落、段评、迁移日志与 base_revision_id。
- `005_creator_workflow.js` 新增 workflow_version、requested_chapter_no 和幂等操作表。
- 004／005 默认导出接收已有连接；仅导入文件或直接运行模块不会自动完成全部数据库准备。
- MySQL DDL 会隐式提交；不能指望正文迁移事务替整个结构变更回滚。先检查实际列、索引、外键和版本，再做必要结构变化。
- 本地库已具备这些结构；其他数据库是否已经应用未知，部署时逐库核查。

### 10.3 服务与上线状态

- 主接口服务：`BE/server.js`。
- 定时发布服务：`BE/environment/定时发布小说/server.js`，每分钟扫描到期申请；需要实际运行并使用新代码。
- 上一轮没有由 AI 重启业务服务或发布正式版本，本次文档任务也没有执行部署。不能把本地构建通过写成“线上已经更新”。
- 先处理 B01 等关键缺口，再安排服务更新和实际联调。避免旧、新审核进程长期同时处理同一批新工作流单据。
- 看定时失败时，检查 scheduled_publish_time、workflow_version、release_status、release_next_retry_time、release_last_error、发布基线与服务是否启动；不要直接改成“已发布”跳过事务。

## 11. 接手后的检查与验收清单

以下是检查方法，不要求接手者不加判断全部立即执行。真实业务迁移不在这些测试命令内。

### 11.1 自动化检查命令

在 APP 目录：

```sh
/Users/ali/fvm/versions/3.44.2/bin/flutter test --no-pub test/pages/work_editor test/pages/author_center
/Users/ali/fvm/versions/3.44.2/bin/dart analyze --format=machine lib
/Users/ali/fvm/versions/3.44.2/bin/flutter build bundle --debug --no-pub
```

在 BE 目录，使用独立临时测试库：

```sh
CREATOR_WORKSPACE_TEST=1 node --test --test-force-exit test/creator_workspace.test.js test/creator_revision_flow.test.js test/novel_content_body_read.test.js test/novel_content_migration.test.js
```

不要用 `npm test` 替代上面的明确测试集合而不先检查脚本。项目默认 test 并不是本工作流的专用入口。上述 MySQL 集成测试会创建并清理自己命名的临时测试库，需本地测试账号具有相应权限；不会启动用户的业务正文迁移程序。

### 11.2 必须补齐的端到端验收

- [ ] 新建短篇，输入 2 万字符，保存、退出、重开，空白和 emoji 完整。
- [ ] 新建长篇 A/B/C 三章，仅提交 A/B，管理后台通过后读者只看到 A/B，C 仍为草稿。
- [ ] 已公开 A 修改后保存／投稿，读者继续读旧 A；正式通过后读新版，章节 ID 不变。
- [ ] 在有旧磁盘／内存缓存的设备上重复上一项，不能靠清缓存或全新设备掩盖问题。
- [ ] 驳回、撤回后新建草稿继续修改，旧审核详情仍保持原投稿文字。
- [ ] 管理后台开始审核后，作者撤回被拒绝，其他未提交章节仍可保存。
- [ ] 定时未到、到期、取消、发布失败重试；包括批准时间已晚于预定时间。
- [ ] 定时等待期间平台下架，不能到时自动绕过下架。
- [ ] 删除章、只提交换序一侧、提交两侧、删除最后一章、重复点击审核。
- [ ] 两设备编辑同章、旧整本编辑对新单章保存、网络响应丢失后继续输入。
- [ ] 离线副本对应修订被别人提交／撤回／驳回，仍能找到并恢复文字。
- [ ] 损坏本机副本、磁盘写失败、账号切换、系统挂起与重开。
- [ ] 不同语种同一本作品分别保存，原文不串读；界面语种改变不误改创作语种。
- [ ] 非作者不能读写草稿；普通作者不能调用管理员审核；未公开、删除或下架内容不能经正文接口旁路读取。
- [ ] 大目录性能、真实外键失败、连接池回滚与审核／撤回／定时任务并发。
- [ ] 段评开发完成后补同段回复、计数事务、换版与 UTF-16 锚点验收。

## 12. 给接手 AI／开发者的执行摘要

本阶段不要另建一套作品表、重新把正文写回 OBS，或把“草稿、审核中、已公开”压成一个字段。沿用现有修订表、独立正文表和提交项模型。

**先修 B01 管理后台旧审核旁路，再修 B02 读者版本缓存。** 然后收紧并发入口、补恢复和实际链路验证，再开发段落评论。已有测试通过是回归基础，不是忽略这些新发现的理由。

每次交接必须留下：修改了哪些入口和表、采用什么状态规则、运行了哪些测试、哪些仍未验证、迁移及服务是否实际运行、剩余风险编号。不要把“代码已经存在”“测试通过”“已部署”“全库迁移完成”写成同一个完成状态。
