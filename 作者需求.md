# 创作者投稿与作品管理需求文档

> 文档状态：第一阶段需求确认稿
>
> 适用项目：Flutter App、Node.js 后端、Vue 后台
>
> 数据库：本地 MySQL `0.0.0.0:3306/novel`
>
> 目标：已认证创作者可以创建、提交、审核、发布并持续维护长篇或短篇小说
>
> 第一阶段明确不包含：AI 辅助写作、AI 续写/改写、作者认证流程重做、签约、稿费结算、提现、复杂版权交易

---

## 1. 需求背景

当前创作者认证链路已经完成：

1. 用户登录后进入创作者入口。
2. 用户填写邮箱、验证码和自我介绍。
3. 后端校验验证码后，把 `subscriber_information.author` 更新为 `2`。
4. Flutter 端通过 `author == 2` 判断用户是已认证创作者。

当前投稿链路尚未完成：

- Flutter 的 `lib/pages/installation/author_view.dart` 仍是“作者展示”占位页面。
- 后端目前只有小说阅读、查询、搜索、评论等读者接口，没有创作者写入接口。
- Vue 后台目前没有作品审核、章节审核、作者作品管理页面。
- 数据库现有小说表主要面向“已经发布给读者的数据”，没有草稿、投稿审核、驳回原因和已发布内容修订版本。

本需求要在不破坏现有阅读链路的前提下，新增完整的创作者投稿能力。

---

## 2. 产品目标

### 2.1 核心目标

- 仅已登录且 `author == 2` 的用户可以创建和提交小说。
- 创作者可以选择长篇或短篇，上传封面，填写标题、简介、语种和分类。
- 长篇小说按章节管理，每章独立保存草稿、提交审核、发布和记录更新时间。
- 短篇小说直接管理一篇完整正文，不创建章节。
- 创作者可以随时保存草稿，草稿不得出现在读者端。
- 投稿必须经过后台审核，审核通过后才能进入现有小说列表、搜索、推荐和阅读链路。
- 已发布作品修改时，读者继续看到上一个已审核版本；新版本审核通过后再切换。
- 后台可以审核作品资料、短篇正文、长篇章节，并记录完整审核历史。
- 审核结果通过站内消息和 FCM 推送通知创作者。

### 2.2 对标番茄小说的范围

本期对标的是基础作者工作台，而不是一次性实现番茄小说全部商业能力：

- 作品创建和资料维护。
- 长篇章节创作和更新。
- 短篇全文创作。
- 草稿、审核、驳回修改、发布。
- 作品和章节状态管理。
- 基础作品数据查看。

签约、稿费、推荐申请、作家等级、福利任务、版权运营、AI 辅助写作、抄袭检测等放到后续阶段。

### 2.3 第一阶段已确认范围

第一阶段必须形成以下完整闭环：

1. 一个创作者可以创建和管理多本小说，不限制为单本作品。
2. 创作者可以随时保存服务端草稿，并在任意已登录设备重新调阅。
3. 作者作品列表同时展示草稿、待审核、已驳回、待发布、已发布和已下架作品。
4. 长篇按章节保存草稿和投稿；短篇保存一份完整正文草稿。
5. 每次准备公开的作品、短篇正文、新章节或章节修订都必须生成审核单。
6. 审核单进入待审核状态后，向审核邮箱 `2209197172@qq.com` 发送一封投稿提醒邮件。
7. Vue 后台提供权威待审核列表；邮件只负责提醒，不代替数据库审核队列。
8. 作者提交时选择一种发布方式：
   - 审核通过后立即自动发布。
   - 审核通过后在作者指定的时间自动发布。
9. 审核通过但未到指定时间的内容处于“已通过，待发布”状态，读者端不可见。
10. 第一阶段不提供任何 AI 生成、续写、改写、润色或自动创作入口。

### 2.4 第一阶段发布方式

| 发布方式 | 业务含义 |
|---|---|
| 审核通过后立即发布 | 审核员通过后，发布事务立即执行 |
| 审核通过后定时发布 | 审核员通过后等待指定时间，由持久化定时任务发布 |

推荐规则：

- 定时时间由作者设备选择，但提交给后端时统一转换为 UTC，并同时记录作者提交时区。
- App 和后台按各自本地时区显示，同时明确显示 UTC 时间，避免跨时区误解。
- 作者选择的定时时间必须晚于提交时间，最小提前量由后台配置，建议默认 10 分钟。
- 如果审核通过时指定时间已经过去，推荐立即发布，不再要求作者重新提交。
- 同一审核单中的批量章节首期使用同一个发布时间。
- 定时待发布期间内容已经审核通过，不允许修改正文；修改正文必须取消本次待发布并重新审核。
- 定时待发布期间允许修改为另一个未来时间，内容不变时不需要重新审核，但必须记录操作日志。
- 定时任务必须以数据库状态为准，服务重启或 PM2 重载后仍能继续发布，不能只依赖内存计时器。

---

## 3. 名词与角色

### 3.1 名词

| 名词 | 定义 |
|---|---|
| 作品 | 一本长篇小说或一篇短篇小说，对应 `novel_main` |
| 语种版本 | 作品在某个语种下的资料，对应 `novel_main_language` |
| 长篇 | 正文由多条章节组成，公开状态兼容现有 `publish_status = 1/2` |
| 短篇 | 正文是一个完整内容文件，不创建章节，公开状态兼容现有 `publish_status = 4` |
| 公开投影 | 当前已经审核通过、可以提供给读者的数据 |
| 修订版本 | 创作者正在编辑或等待审核的资料/正文快照 |
| 审核单 | 一次提交给后台审核的不可变内容集合 |
| 首次投稿 | 作品第一次从草稿提交并申请公开 |
| 更新投稿 | 已发布作品的资料、短篇正文或章节发生修改后再次提交 |

### 3.2 角色

| 角色 | 权限 |
|---|---|
| 普通用户 | 只能阅读，不允许访问创作者写接口 |
| 已认证创作者 | 只能创建和管理自己名下的作品、草稿和投稿 |
| 内容审核员 | 查看待审内容，批准或驳回，不允许修改作者原稿 |
| 内容管理员 | 拥有审核、下架、恢复、分类调整和异常处理权限 |
| 超级管理员 | 配置审核规则、分类、上传限制和后台角色权限 |

---

## 4. 已有数据库能力与结论

### 4.1 可以复用的表

| 表名 | 复用方式 |
|---|---|
| `subscriber_information` | 使用 `author = 2` 判断创作者身份 |
| `novel_main` | 继续作为作品主体和读者可见的公开状态、统计数据 |
| `novel_main_language` | 继续保存已发布的语种标题、简介、封面和短篇正文 URL |
| `novel_chapter` | 继续保存已审核发布的长篇章节 |
| `novel_preference_type` | 继续作为分类组配置 |
| `novel_preference` | 继续作为具体分类/标签配置 |
| `novel_main_category_rel` | 继续保存已发布作品与分类的关系 |
| `novel_message` | 复用为审核通过、驳回和下架通知 |
| `fcm_token` | 复用为审核结果推送设备来源 |
| `novel_certification` | 只用于认证验证码，本需求不改变 |

### 4.2 现有表不能直接解决的问题

`novel_main.publish_status` 当前同时表达：

- `1`：长篇连载中。
- `2`：长篇已完结。
- `3`：下架。
- `4`：短篇。

它混合了“作品类型”“连载状态”和“公开状态”，不能再加入草稿、待审核、驳回等状态，否则会破坏现有搜索、推荐、阅读和跳转逻辑。

`novel_chapter` 当前只有已删除状态和发布时间，缺少：

- 章节草稿。
- 待审核状态。
- 驳回原因。
- 定时发布。
- 已发布章节修改前后的版本。
- 审核人员和审核时间。

如果作者直接更新 `novel_chapter.content_url`，待审正文会立刻覆盖读者正在阅读的正文，因此必须增加独立的修订版本。

### 4.3 数据库结论

需要新增投稿相关表，并给已有表增加少量兼容字段。现有 `novel_main`、`novel_main_language`、`novel_chapter` 只承载审核通过的公开数据；作者草稿和待审内容存放在修订表中。

---

## 5. 推荐的数据模型

## 5.1 `novel_main` 新增字段

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `work_type` | tinyint | `1=长篇`，`2=短篇`，不得用分类字段代替 |
| `serialization_status` | tinyint | `1=连载中`，`2=已完结`；短篇固定为 `2` |
| `public_status` | tinyint | `1=未公开`，`2=已公开`，`3=作者下架`，`4=平台下架` |
| `initial_audit_status` | tinyint | `1=草稿`，`2=待审核`，`3=通过`，`4=驳回` |
| `original_language_id` | int nullable | 原始创作语种，关联 `language_list.id`；历史作品无法确认时允许为空 |
| `first_publish_time` | datetime nullable | 第一次公开发布时间 |
| `last_publish_time` | datetime nullable | 最近一次新内容公开时间 |
| `last_submission_id` | bigint nullable | 最近一次审核单 ID，便于作者列表快速展示 |
| `creator_update_time` | datetime | 作者最后一次编辑草稿的时间 |
| `lock_version` | int unsigned | 乐观锁版本，防止多设备编辑互相覆盖 |

必须增加索引：

- `(author_id, remove_status, creator_update_time)`：作者作品列表。
- `(public_status, latest_update_time)`：公开作品筛选。
- `(initial_audit_status, create_time)`：首次投稿审核筛选。

### 5.1.1 与旧 `publish_status` 的兼容映射

| 条件 | `publish_status` |
|---|---:|
| 已公开 + 长篇 + 连载中 | 1 |
| 已公开 + 长篇 + 已完结 | 2 |
| 未公开/作者下架/平台下架 | 3 |
| 已公开 + 短篇 | 4 |

`publish_status` 暂时保留给现有 App 阅读逻辑使用，新创作者接口不得把它当成完整投稿状态。

## 5.2 `novel_main_language` 调整

建议新增：

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `published_revision_id` | bigint nullable | 当前公开资料对应的修订版本 |
| `latest_draft_revision_id` | bigint nullable | 当前可继续编辑的草稿版本 |

约束：

- 长篇的 `content_url` 必须保持为空，正文只能存在章节中。
- 短篇的 `content_url` 只保存审核通过后的正文文件 URL。
- `cover_width` 和 `cover_height` 必须由服务端读取真实图片后写入，不信任客户端。

## 5.3 `novel_chapter` 调整

建议新增：

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `published_revision_id` | bigint nullable | 当前公开章节对应的修订版本 |
| `volume_id` | bigint nullable | 可选卷 ID；首期不启用卷时保持为空 |

现有时间字段语义必须固定：

- `create_time`：公开章节记录创建时间。
- `publish_time`：章节第一次公开时间，后续修订不得覆盖。
- `update_time`：最近一次审核通过并更新公开正文的时间。
- 修订草稿更新时间不写入这里，只写入修订表。

## 5.4 新增 `novel_work_revision`（必须）

保存作品资料和短篇正文的完整修订快照。

核心字段：

| 字段 | 说明 |
|---|---|
| `id` | 修订版本 ID |
| `novel_id` | 作品 ID |
| `novel_language_id` | 语种版本 ID |
| `author_id` | 作者 ID，必须与 `novel_main.author_id` 一致 |
| `revision_no` | 作品内递增版本号 |
| `work_type` | 长篇/短篇快照 |
| `serialization_status` | 连载状态快照 |
| `language_id` | 原始语种快照 |
| `title` | 标题快照 |
| `subtitle` | 副标题快照 |
| `introduction` | 简介快照 |
| `cover_asset_id` | 封面资源 ID |
| `cover_url` | 审核快照使用的封面 URL |
| `cover_width/cover_height` | 封面真实尺寸 |
| `short_content_asset_id` | 短篇正文资源 ID，长篇为空 |
| `content_url` | 短篇正文 URL，长篇为空 |
| `word_count` | 服务端计算的短篇字数 |
| `category_snapshot` | 投稿时分类 ID、分类名称和分组的 JSON 快照 |
| `revision_status` | `1=草稿`，`2=已锁定待审`，`3=已发布`，`4=驳回`，`5=已废弃` |
| `content_hash` | 资料与正文摘要，用于幂等和重复提交检测 |
| `create_time/update_time` | 草稿创建和最近保存时间 |

规则：

- 自动保存只更新当前草稿，不得每次输入都新增一条记录。
- 点击提交时锁定当前版本，锁定后不可修改。
- 驳回后基于被驳回版本复制出新的草稿版本。
- 已发布版本必须永久保留，用于审核追溯和问题回滚。

## 5.5 新增 `novel_chapter_revision`（必须）

保存长篇章节草稿和每次修订。

核心字段：

| 字段 | 说明 |
|---|---|
| `id` | 章节修订 ID |
| `chapter_uid` | 逻辑章节 UUID，首次发布前后保持不变，用于新章节在没有 `chapter_id` 时关联多次修订 |
| `novel_id` | 所属作品 ID，便于权限和列表查询 |
| `novel_language_id` | 所属语种版本 ID |
| `chapter_id` | 已发布章节 ID；新章节提交前允许为空 |
| `author_id` | 作者 ID |
| `revision_no` | 同一章节递增版本号 |
| `change_type` | `1=新增`，`2=修改`，`3=删除申请` |
| `chapter_no` | 章节序号快照 |
| `volume_id` | 可选卷 ID |
| `title` | 章节标题 |
| `content_asset_id` | 正文资源 ID |
| `content_url` | 正文内部 URL，不允许通过列表/详情接口返回客户端 |
| `word_count` | 服务端计算字数 |
| `content_hash` | 正文摘要 |
| `revision_status` | `1=草稿`，`2=已锁定待审`，`3=已发布`，`4=驳回`，`5=已废弃` |
| `scheduled_publish_time` | 审核通过后的计划发布时间；立即发布时为空，定时发布时必填 |
| `create_time/update_time` | 草稿创建和最近保存时间 |

必须增加索引：

- `(author_id, revision_status, update_time)`。
- `(novel_id, revision_status, chapter_no)`。
- `(chapter_id, revision_no)`。
- 同一章节同时最多只能存在一个可编辑草稿和一个待审版本，此规则由事务保证。

## 5.6 新增 `novel_creator_submission`（必须）

审核单主表。一次首次投稿可以同时包含作品资料和多章正文。

核心字段：

| 字段 | 说明 |
|---|---|
| `id` | 审核单 ID |
| `submission_no` | 对外展示的唯一审核单号 |
| `author_id` | 作者 ID |
| `author_name/author_email` | 提交时作者名称和联系邮箱快照 |
| `novel_id` | 作品 ID |
| `submission_type` | `1=首次投稿`，`2=资料更新`，`3=新增章节`，`4=章节修改`，`5=章节删除`，`6=完结申请`，`7=重新上架申请` |
| `status` | `1=待审核`，`2=审核中`，`3=通过`，`4=驳回`，`5=作者撤回` |
| `release_mode` | `1=审核通过后立即发布`，`2=审核通过后定时发布` |
| `release_status` | `1=等待审核`，`2=审核通过待发布`，`3=已发布`，`4=已取消发布`，`5=发布失败待重试` |
| `scheduled_publish_time` | UTC 定时时间；立即发布时为空 |
| `submit_timezone` | 作者提交时的 IANA 时区，如 `Asia/Shanghai` |
| `actual_publish_time` | 实际公开时间 |
| `release_retry_count/next_retry_time/last_error` | 定时发布失败重试状态 |
| `submit_note` | 作者给审核员的说明 |
| `reject_code` | 标准驳回原因编码 |
| `review_note` | 审核说明；驳回时必填且对作者可见 |
| `reviewer_id` | 审核员 ID |
| `rights_statement_version` | 作者提交时同意的权利声明版本 |
| `rights_confirm_time` | 作者确认权利声明的时间 |
| `submit_ip` | 提交 IP，用于安全和版权追溯 |
| `review_email_status` | `1=待发送`，`2=已发送`，`3=发送失败待重试` |
| `review_email_to` | 本审核单实际提醒收件邮箱快照 |
| `review_email_send_time` | 审核提醒邮件发送成功时间 |
| `review_email_retry_count` | 邮件重试次数 |
| `review_email_last_error` | 最近一次脱敏错误摘要，不保存 SMTP 密码 |
| `submit_time` | 提交时间 |
| `review_time` | 审核完成时间 |
| `create_time/update_time` | 记录时间 |

必须增加索引：

- `(status, submission_type, submit_time)`：后台待审队列。
- `(author_id, status, submit_time)`：作者投稿记录。
- `(novel_id, status, submit_time)`：作品审核历史。
- `(release_status, scheduled_publish_time)`：定时发布扫描。
- `(review_email_status, update_time)`：投稿提醒邮件失败重试。

审核时必须使用状态条件更新，确保两个审核员不能重复审核同一审核单。

一个作者可以关联任意多条 `novel_main` 记录。禁止给 `novel_main.author_id` 增加唯一索引；作品数量限制只能作为可配置的防滥用规则，不能写成“一位作者只能有一本书”。

## 5.7 新增 `novel_creator_submission_item`（必须）

审核单与修订版本的关联表。

核心字段：

| 字段 | 说明 |
|---|---|
| `submission_id` | 审核单 ID |
| `target_type` | `1=作品修订`，`2=章节修订` |
| `target_id` | 对应修订表 ID |
| `create_time` | 创建时间 |

唯一索引：`(submission_id, target_type, target_id)`。

## 5.8 新增 `novel_creator_asset`（必须）

记录创作者上传资源的归属、用途和安全状态，不能让客户端直接提交任意公网 URL。

核心字段：

| 字段 | 说明 |
|---|---|
| `id` | 资源 ID |
| `author_id` | 上传者 ID |
| `novel_id` | 绑定作品，创建作品前允许为空 |
| `asset_type` | `1=封面`，`2=长篇章节正文`，`3=短篇正文` |
| `storage_provider` | 存储服务标识 |
| `object_key` | 对象存储内部 key |
| `file_url` | CDN URL |
| `mime_type` | 服务端检测 MIME |
| `file_size` | 文件字节数 |
| `width/height` | 图片资源真实尺寸，正文资源为 0 |
| `sha256` | 文件摘要 |
| `reference_status` | `1=临时`，`2=已引用`，`3=已删除` |
| `safety_status` | `1=待检测`，`2=通过`，`3=拒绝` |
| `expire_time` | 临时资源回收时间 |
| `create_time/update_time` | 记录时间 |

服务端保存作品或章节时只接收 `asset_id`，再校验资源归属、类型和安全状态；不得信任客户端传入的 `content_url`。

## 5.9 分类表调整

`novel_preference_type` 中不仅有题材，还包含首页分类、榜单、热门搜索等系统配置，不能全部展示给作者。

建议新增：

| 字段 | 说明 |
|---|---|
| `creator_show_status` | `1=作者可见`，`2=作者不可见` |
| `creator_required` | `1=选填`，`2=必填` |
| `creator_min_select` | 最少选择数量 |
| `creator_max_select` | 最多选择数量 |

`novel_main_category_rel` 建议新增：

| 字段 | 说明 |
|---|---|
| `relation_type` | `1=主分类`，`2=标签` |
| `sorting` | 作品详情中分类展示顺序 |

规则：

- 长篇/短篇是业务类型，不得只用“篇幅偏好”分类判断。
- 连载中/已完结是业务状态，不得只用“完结偏好”分类判断。
- 审核通过时，服务端根据 `work_type` 和 `serialization_status` 自动同步对应篇幅/完结标签；作者不能伪造这些系统标签。
- 首期至少设置一个主内容分类，可再选择若干标签，数量由服务端配置返回。

## 5.10 可选新增 `novel_volume`

卷管理不是首期上线阻塞项，但长篇规模较大后建议支持。

核心字段：`id`、`novel_language_id`、`volume_no`、`title`、`introduction`、`sorting`、`remove_status`、`create_time`、`update_time`。

首期不做卷时，章节直接按 `chapter_no` 排列；后续增加卷不能改变现有章节 ID 和读者进度。

---

## 6. 状态机与业务规则

## 6.1 首次投稿状态机

```text
草稿
  ├─ 提交成功 → 待审核
  ├─ 作者删除 → 软删除
  └─ 保存失败 → 保持草稿并提示重试

待审核
  ├─ 审核通过 + 立即发布 → 已公开
  ├─ 审核通过 + 定时发布 → 已通过待发布 → 到时自动公开
  ├─ 审核驳回 → 已驳回，可复制为新草稿修改
  └─ 审核开始前作者撤回 → 草稿
```

### 首次投稿通过时

必须在同一个数据库事务内完成：

1. 锁定审核单并确认状态仍可审核。
2. 校验作品、语种、资源、分类和作者状态。
3. 立即发布模式：把修订复制到公开表并完成公开状态切换。
4. 定时发布模式：把审核单标记为“审核通过待发布”，修订继续保持不可变且读者不可见。
5. 实际发布时把作品修订复制到 `novel_main`、`novel_main_language` 和分类关系表。
6. 长篇把本次审核通过的章节修订写入 `novel_chapter`。
7. 短篇把审核通过的正文 URL 写入 `novel_main_language.content_url`。
8. 重新计算章节数、字数、最新章节号和公开时间。
9. 更新兼容字段 `publish_status`、审核单 `release_status` 和 `actual_publish_time`。
10. 提交事务后再发送站内通知和 FCM；通知失败不得回滚已发布内容。

### 定时发布任务

- 后端定时扫描 `release_status = 2` 且 `scheduled_publish_time <= UTC_TIMESTAMP()` 的审核单。
- 使用行锁或原子条件更新领取任务，同一审核单只能发布一次。
- 发布逻辑必须复用立即发布的公共发布服务，不能维护两套发布 SQL。
- 单次失败写入 `release_status = 5` 并记录脱敏错误，按退避策略自动重试。
- 任务恢复后先补发所有已到期但未发布的审核单。
- 实际发布时间允许有调度误差，第一阶段验收建议不超过 2 分钟。

## 6.2 已发布资料修改

- 作者编辑的是新修订版本，不能直接更新公开表。
- 待审核期间，读者继续看到旧标题、旧简介和旧封面。
- 审核通过后一次性替换公开资料。
- 审核驳回后公开资料不变，作者看到明确的驳回原因。

## 6.3 长篇章节状态机

```text
章节草稿 → 待审核 → 立即发布/审核通过待发布 → 已发布
              └──→ 已驳回 → 新草稿
```

已发布章节修改：

```text
公开版本 A + 修改草稿 B
修改草稿 B → 待审核
  ├─ 通过：公开版本切换到 B，A 保留为历史版本
  └─ 驳回：继续公开 A，B 标记驳回
```

规则：

- 已发布章节的 `chapter_no` 原则上不可由作者修改。
- 新章节编号在审核发布事务中分配或最终确认，必须使用数据库锁避免并发重复。
- 公开章节不能硬删除；作者只能发起删除/下架申请。
- 草稿章节可以软删除。
- 已完结作品默认不允许新增章节，需要先由管理员恢复为连载中。
- 首期所有新章节和修改章节都经过审核；以后可以增加可信作者快速审核策略，但不能绕过内容安全记录。

## 6.4 短篇规则

- 短篇只有一份完整正文，不创建 `novel_chapter`。
- `novel_main_language.chapter_count` 固定为 `0`。
- 首次提交时作品资料、封面、分类和完整正文组成一个审核单。
- 已发布短篇修改正文时，旧正文继续可读，审核通过后才替换 `content_url`。
- 替换正文后必须清除 Redis `novel:content:v1:short_story:{novel_language_id}` 缓存。

## 6.5 长篇统计更新时间规则

- 章节 `publish_time`：第一次公开时间，永久保留。
- 章节 `update_time`：最近一次审核通过的公开修订时间。
- 章节修订 `update_time`：作者最近保存草稿时间。
- `novel_main.latest_update_time`：最近一次“新章节公开”时间。
- 修改错别字等已有章节修订时，不更新 `novel_main.latest_update_time`，避免通过反复修改旧章节刷新榜单。
- `novel_main.last_publish_time`：新章节或短篇新版本实际公开时间。

---

## 7. 投稿字段和校验规则

所有限制必须由后端配置接口返回，Flutter 只用于即时提示，后端再次做权威校验。以下是首期建议默认值，开发时集中放入配置文件，不允许散落魔法数字。

## 7.1 作品资料

| 字段 | 长篇 | 短篇 | 建议校验 |
|---|---:|---:|---|
| 作品类型 | 必填 | 必填 | 创建后有公开内容时不可自行切换 |
| 原始语种 | 必填 | 必填 | 仅允许 `language_list` 中启用语种 |
| 标题 | 必填 | 必填 | 1～80 个 Unicode 字符，去除首尾空白 |
| 副标题 | 选填 | 选填 | 0～120 个 Unicode 字符 |
| 简介 | 必填 | 必填 | 建议 20～2000 个字符 |
| 封面 | 必填 | 必填 | 必须通过资源上传接口 |
| 来源 | 必填 | 必填 | 创作者投稿首期只允许原创；转载需后台单独授权 |
| 主分类 | 必填 | 必填 | 只能选择作者可见分类 |
| 标签 | 选填 | 选填 | 最大数量由配置返回 |
| 连载状态 | 必填 | 固定已完结 | 长篇为连载中/已完结 |
| 发布方式 | 必填 | 必填 | 审核通过立即发布或审核通过定时发布 |
| 定时时间 | 条件必填 | 条件必填 | 选择定时发布时必填，服务端保存 UTC |
| 权利声明 | 必填勾选 | 必填勾选 | 声明确有发布权且遵守内容规范 |

## 7.2 封面

- 建议统一使用竖版 `3:4`，推荐尺寸 `720×960`，最低尺寸 `600×800`。
- 支持 JPEG、PNG、WebP，服务端按文件头识别真实格式。
- 建议原文件不超过 10 MB，限制值由配置接口下发。
- 上传后提供裁剪预览，输出统一 WebP，同时保存实际宽高。
- 禁止 GIF、SVG、可执行文件和伪装扩展名。
- 图片必须经过内容安全检测；检测未通过不能提交审核。
- 作者更换封面并被驳回时，公开作品继续使用旧封面。

## 7.3 长篇章节

| 字段 | 建议规则 |
|---|---|
| 章节标题 | 必填，1～100 个字符 |
| 正文 | 提交审核时必填；草稿允许未达到最低字数 |
| 章节序号 | 由服务端管理，客户端只展示 |
| 字数 | 服务端从标准化后的纯文本重新计算 |
| 发布时间 | 提交审核时选择审核通过立即发布或指定时间发布 |

首期建议单章提交范围为 500～20000 个有效字符/词，具体上下限必须支持后台配置。首次长篇投稿至少包含一章达到提交条件的章节。

## 7.4 短篇正文

- 首期只支持纯文本 UTF-8，不支持 HTML、JavaScript 或任意富文本标签。
- 建议提交范围为 1000～50000 个有效字符/词，具体由后台配置。
- 换行统一为 `\n`，去除不可见控制字符，保留正常段落和全角标点。
- 客户端显示的字数只作为预估，最终以服务端计算结果为准。

## 7.5 多语种字数

必须提供一个后端公共字数计算函数并编写单元测试：

- CJK 语种按有效文字字符计数。
- 英语、法语、斯瓦希里语等按单词 token 计数。
- 标点和纯空白不计入。
- 计算规则根据 `language_id` 选择，不根据客户端传入的任意字符串选择。
- Flutter 可实现同规则用于预览，但数据库值始终以服务端为准。

---

## 8. Flutter App 功能清单

## 8.1 创作者中心首页

替换现有 `AuthorView` 占位内容，至少展示：

- 新建作品按钮。
- 我的作品入口。
- 草稿数量。
- 待审核数量。
- 已驳回数量。
- 已发布作品数量。
- 最近编辑作品/章节。
- 审核通知入口。
- 基础数据总览：作品数、总阅读、总点赞、总收藏。

### 8.1.1 `user_info` 作者统计卡片

当前普通用户统计区展示评论消息、点赞消息、收藏消息。第一阶段按身份分流：

| 用户身份 | 第一张 | 第二张 | 第三张 |
|---|---|---|---|
| 普通用户 | 保留当前评论统计 | 保留当前点赞统计 | 保留当前收藏统计 |
| 已认证作者 | 小说数量 | 小说收藏数量 | 评论数量 |

作者统计口径建议固定为：

- 小说数量：当前作者所有 `remove_status = 1` 的作品数，包含草稿、待审核、已驳回、审核通过待发布、已发布和已下架作品。
- 小说收藏数量：作者名下所有“至少公开过一次”的作品当前 `favorite_count` 之和；用户取消收藏后该数值相应减少，草稿和从未公开作品不计入。
- 评论数量：作者名下所有“至少公开过一次”的作品当前有效 `comment_count` 之和，与作品现有评论口径保持一致，包含顶层评论和回复，已软删除评论不计入。
- 三个数值均由后端按当前登录用户计算，客户端不得传 `author_id`。
- 统计接口需要返回服务端统计时间；缓存计数异常时应能从收藏表和评论表重新校准。

点击行为：

- 点击“小说数量”进入“我的作品”列表，列表必须包含服务端保存的草稿。
- 点击“小说收藏数量”建议进入消息页并筛选小说收藏消息。
- 点击“评论数量”建议进入消息页并筛选作品评论/回复消息。
- 收藏和评论卡片可继续显示对应未读角标，但卡片主数值使用作品累计数据，不再使用消息条数。

作者身份变化、创建/删除作品、收到收藏或评论后，需要刷新作者统计；接口失败时保留上次成功值并提供下拉刷新。

## 8.2 我的作品列表

筛选 Tab：全部、草稿、待审核、审核通过待发布、已发布、已驳回、已下架。

每个作品项显示：

- 封面和标题。
- 长篇/短篇。
- 连载中/已完结。
- 当前投稿状态。
- 公开章节数和总字数。
- 最近草稿更新时间。
- 最近公开更新时间。
- 发布方式和计划发布时间。
- 阅读、点赞、收藏和评论数量。
- 驳回时显示驳回原因摘要和“修改后重新提交”。

列表必须分页，不能一次加载作者所有作品。

## 8.3 新建作品流程

推荐分步表单：

1. 选择长篇或短篇。
2. 选择原始语种。
3. 填写标题、副标题和简介。
4. 上传并裁剪封面。
5. 选择主分类和标签。
6. 长篇进入章节管理；短篇进入全文编辑。
7. 预览投稿内容。
8. 选择审核通过立即发布或审核通过定时发布；定时发布时选择时间。
9. 勾选权利声明并提交审核。

作品创建后立即获得 `novel_id` 和草稿修订 ID，后续上传的资源全部绑定该作品。

## 8.4 作品资料编辑

- 自动保存当前草稿。
- 显示“已保存/保存中/保存失败”。
- App 进入后台前主动保存一次。
- 离线时保存到本地草稿队列，恢复网络后提示用户同步。
- 服务端返回版本冲突时不得静默覆盖，提示用户选择重新加载或保留本机副本。
- 待审核版本只读；作者撤回后才能继续编辑。
- 审核通过待发布版本只读；允许只调整未来发布时间并记录操作日志。
- 已发布作品编辑时明确提示“修改审核通过前，读者仍看到旧版本”。

## 8.5 长篇章节管理

章节列表显示：

- 章节序号。
- 章节标题。
- 字数。
- 状态：草稿、待审核、审核通过待发布、已发布、已驳回。
- 草稿最近保存时间。
- 第一次发布时间。
- 公开内容最近更新时间。
- 驳回原因。
- 发布方式、计划发布时间和实际发布时间。

支持：

- 新建章节。
- 编辑草稿。
- 编辑已发布章节并生成新修订。
- 删除未提交草稿。
- 提交单章审核。
- 批量选择草稿章节组成一次审核单。
- 提交时选择立即发布或定时发布。
- 对审核通过待发布章节调整计划发布时间或取消待发布。
- 预览章节。
- 从驳回版本继续修改。

首期不允许作者调整已发布章节顺序；未发布草稿可以在提交前排序。

## 8.6 章节编辑器

- 标题输入。
- 纯文本正文输入。
- 实时预估字数。
- 每隔约 3 秒空闲自动保存，实际间隔由统一配置控制。
- 手动保存按钮。
- 撤销/重做使用 Flutter 本地编辑能力。
- 离开页面前检查未保存内容。
- 大正文输入需要避免每次按键重建整个页面。
- 保存接口使用 `client_version`/`lock_version` 防止覆盖。
- 提交后编辑器切换只读，展示审核状态。

## 8.7 短篇编辑器

- 与章节编辑器复用正文输入、字数、自动保存、冲突处理和预览能力。
- 短篇编辑器编辑的是作品修订，不创建伪章节。
- 正文审核通过前，短篇不得进入短篇推荐列表。

## 8.8 审核记录

按时间倒序展示：

- 审核单号。
- 投稿类型。
- 包含的资料或章节。
- 提交时间。
- 当前状态。
- 审核完成时间。
- 审核人员展示名（可选）。
- 驳回原因和整改建议。

## 8.9 通知跳转

- 作品通过：跳转作品管理页。
- 作品驳回：跳转被驳回修订详情。
- 章节通过：跳转章节列表并定位章节。
- 章节驳回：跳转章节编辑页并展示原因。
- 平台下架：跳转作品状态页并展示下架原因。

## 8.10 App 适配要求

- 所有新增页面必须适配日间和夜间主题。
- 所有文字进入 i18n，不允许直接硬编码中文。
- CJK 和非 CJK 文案长度分别适配。
- 字重统一使用 `FontConfig.adjustedWeight()`。
- 页面 UI、逻辑、样式和子组件按项目规范拆分。
- 上传、自动保存和提交过程必须有防重复点击和可恢复错误提示。

---

## 9. Node.js 后端功能清单

项目现有接口以 POST 为主，新接口继续保持一致。目录名和文件名使用小写下划线，一个文件尽量只实现一个接口或公共函数。

## 9.1 创作者权限中间件

新增统一校验函数，所有创作者写接口必须执行：

1. 校验有效登录 Token。
2. 从服务端用户信息读取当前用户 ID。
3. 校验用户未删除、未禁用。
4. 校验 `subscriber_information.author == 2`。
5. 涉及作品时校验 `novel_main.author_id == 当前用户 ID`。
6. 涉及资源时校验 `novel_creator_asset.author_id == 当前用户 ID`。
7. 不允许客户端传入 `author_id` 决定归属。

## 9.2 创作配置接口

`creator_work/get_form_config`

返回：

- 可投稿语种。
- 长篇/短篇类型。
- 作者可见分类分组和选择规则。
- 封面尺寸、格式和大小限制。
- 标题、简介、章节、短篇正文限制。
- 自动保存建议时间。
- 允许的发布方式、最小定时提前量和服务端当前时间。
- 当前服务端功能开关。

## 9.3 作品接口

| 接口 | 作用 |
|---|---|
| `creator_work/create_draft` | 创建作品和首个作品草稿 |
| `creator_work/inquire` | 分页查询当前作者作品 |
| `creator_work/get_info` | 查询作品公开数据、当前草稿和状态 |
| `creator_work/save_draft` | 保存作品资料或短篇正文草稿 |
| `creator_work/submit` | 提交首次投稿或资料/短篇修订 |
| `creator_work/withdraw_submission` | 审核开始前撤回审核单 |
| `creator_work/reschedule` | 调整审核通过待发布作品的未来发布时间 |
| `creator_work/cancel_scheduled_release` | 取消尚未执行的定时发布，正文保持不可见 |
| `creator_work/request_complete` | 长篇申请完结 |
| `creator_work/request_off_shelf` | 作者申请下架已发布作品 |
| `creator_work/delete_draft` | 软删除从未公开且没有待审单的草稿 |
| `creator_work/dashboard` | 返回基础创作者数据统计 |
| `creator_work/user_info_statistics` | 返回 user_info 作者态三项统计 |

## 9.4 章节接口

| 接口 | 作用 |
|---|---|
| `creator_chapter/inquire` | 分页查询作者作品章节及修订状态 |
| `creator_chapter/create_draft` | 创建新章节草稿 |
| `creator_chapter/get_info` | 查询公开版本和当前草稿 |
| `creator_chapter/save_draft` | 保存章节草稿 |
| `creator_chapter/submit` | 提交单章审核 |
| `creator_chapter/batch_submit` | 多章组成一个审核单 |
| `creator_chapter/reschedule` | 调整审核通过待发布章节的未来发布时间 |
| `creator_chapter/cancel_scheduled_release` | 取消尚未执行的章节定时发布 |
| `creator_chapter/delete_draft` | 删除未提交草稿 |
| `creator_chapter/create_edit_revision` | 从已发布章节创建修改草稿 |
| `creator_chapter/request_remove` | 申请删除/下架公开章节 |

## 9.5 审核记录接口

| 接口 | 作用 |
|---|---|
| `creator_submission/inquire` | 查询当前作者审核单列表 |
| `creator_submission/get_info` | 查询审核单项目、状态和原因 |

审核单列表和详情必须返回 `release_mode`、`release_status`、计划发布时间、提交时区和实际发布时间。

## 9.6 上传接口

推荐流程：

1. `creator_asset/prepare_upload`：校验作者身份和文件声明，返回受限上传凭证或上传地址。
2. 客户端上传到指定对象存储目录。
3. `creator_asset/complete_upload`：后端读取真实文件信息、计算摘要、处理封面、登记资源。
4. 保存草稿时只提交 `asset_id`。
5. 定时任务清理过期且未引用的临时资源。

安全要求：

- 创作者接口不能直接复用不受限的后台管理员上传权限。
- 上传凭证只能写入当前作者的临时目录，设置短有效期、类型和大小限制。
- 正文 URL 必须位于 `novel_content` 已允许的 HTTPS CDN 主机。
- 正文源 URL 永远不通过作品、章节、审核列表接口返回给 App。
- 下载/预览正文通过鉴权接口读取，不允许客户端自行访问内部 URL。

## 9.7 提交审核校验

服务端必须在事务内检查：

- 作者身份和作品归属。
- 作品未被平台冻结或删除。
- 当前 `lock_version` 与客户端一致。
- 没有冲突的待审版本。
- 作品类型与正文结构一致。
- 长篇至少存在满足要求的章节修订。
- 短篇存在满足要求的正文资源且没有章节。
- 封面、正文资源属于当前作者和当前作品。
- 资源安全状态通过。
- 标题、简介、分类、字数满足配置。
- `release_mode` 只能是立即发布或定时发布。
- 定时发布必须携带合法的 UTC 时间和 IANA 时区，并满足最小提前量。
- 同一幂等键没有创建过审核单。

提交成功后锁定修订内容；后续编辑必须创建新草稿，不能修改审核快照。

## 9.8 审核发布服务

审核通过逻辑必须抽成公共服务，后台接口不能在控制器里零散拼接更新 SQL。

发布服务职责：

- 行级锁定审核单和作品。
- 校验状态流转合法。
- 根据 `release_mode` 决定立即执行发布事务，或只进入审核通过待发布队列。
- 复制修订到公开表。
- 安全更新分类关系。
- 新章节原子分配/确认 `chapter_no`。
- 维护 `chapter_count`、`word_count`、`latest_chapter_no`、`latest_update_time`。
- 维护 `work_type`、`serialization_status`、`public_status` 和兼容 `publish_status`。
- 更新资源引用状态。
- 写审核结果和操作日志。
- 事务提交后清理正文 Redis 缓存。
- 事务提交后发送站内消息和 FCM。

## 9.9 缓存处理

当前正文 Redis Key：

- 长篇：`novel:content:v1:chapter:{chapter_id}`。
- 短篇：`novel:content:v1:short_story:{novel_language_id}`。

章节或短篇修订发布后必须主动删除对应缓存，不能只等待 3 天 TTL，否则审核通过后读者仍可能读到旧正文。

作品资料发布后还要清理受影响的详情、搜索、推荐缓存；如果这些模块目前没有缓存，也要保留统一失效函数，便于后续扩展。

## 9.10 现有读者接口必须补强

现有 `novel/get_info` 只按作品 ID 和删除状态查询，可能读取 `publish_status = 3` 的作品。引入草稿作品后必须统一限制读者接口只能访问公开状态。

需要审查并补强：

- `novel/get_info`。
- `novel/short_story`。
- `novel/short_story_read`。
- `novel/short_story_read_preview`。
- `novel_chapter/inquire`。
- `novel_content/read`。
- 搜索、榜单、个性化推荐。
- 收藏、阅读历史、消息跳转返回的作品信息。

统一公开条件建议为：

```text
remove_status = 1
AND public_status = 2
AND initial_audit_status = 3
AND publish_status IN (1, 2, 4)
```

章节目录还必须只查询已经写入 `novel_chapter` 的公开章节，不能查询修订表。

## 9.11 限流、幂等和审计

- 创建作品、上传资源、保存草稿、提交审核分别限流。
- 保存草稿使用幂等的“更新当前草稿”方式。
- 提交审核必须携带幂等键，网络重试不能创建重复审核单。
- 审核通过/驳回使用条件更新，重复请求只能成功一次。
- 管理员审核、下架、恢复都写操作日志，包括操作者、时间、对象、原状态、新状态和原因。
- 错误日志不得打印正文、邮箱验证码、上传凭证和数据库密码。

## 9.12 待审核邮件提醒

参考现有 `/Users/ali/work/小说/后端/utils/阿里邮箱/发送验证.js` 和作者认证邮件模板机制扩展新的“创作者投稿待审核”邮件类型，不得把验证码模板直接拼接成投稿邮件。

收件规则：

- 第一阶段固定收件邮箱：`2209197172@qq.com`。
- 收件邮箱放在后端配置或环境变量中，禁止由 App 请求参数指定。
- 后续支持配置多个审核邮箱时使用服务端白名单数组。

触发规则：

- 每个审核单第一次进入“待审核”状态后发送一封邮件。
- 首次作品、资料修改、短篇正文、新章节、章节修改、章节删除、完结和重新上架审核都需要提醒。
- 批量章节属于一个审核单，只发送一封汇总邮件，避免邮件轰炸。
- 作者撤回后重新提交会生成新的审核单号，并发送新的邮件。
- 相同审核单的接口重试不得重复发送邮件。

邮件内容至少包含：

- 审核单号和投稿类型。
- 作者 ID、作者名称、作者联系邮箱。
- 作品 ID、作品标题、长篇/短篇、语种和分类。
- 本次包含的章节数量、章节标题摘要和总字数。
- 提交时间。
- 发布方式。
- 定时发布时的 UTC 时间、作者时区和换算后的北京时间。
- 后台审核详情深链接。

邮件中不得包含完整正文、内部 `content_url`、上传凭证、用户 Token 或数据库信息。

可靠性规则：

1. 数据库事务先成功创建审核单，并把 `review_email_status` 写为待发送。
2. 事务提交后立即尝试发送邮件。
3. 邮件发送成功后写入发送时间。
4. 邮件发送失败时审核单仍然有效，后台待审列表仍能看到；状态改为失败待重试。
5. 后台任务按退避策略重试，达到阈值后在后台显示醒目标记，并允许管理员手动重发。
6. 邮件提醒不是审核数据源，任何邮件丢失都不能导致投稿丢失或重复发布。

---

## 10. Vue 后台功能清单

当前后台没有小说投稿审核模块，需要新增菜单、路由、API 和角色权限。

## 10.1 待审队列

筛选条件：

- 审核状态。
- 投稿类型。
- 长篇/短篇。
- 语种。
- 主分类。
- 作者 ID、账号或作者名。
- 作品 ID 或标题。
- 提交时间范围。
- 审核员。
- 发布方式。
- 计划发布时间范围。
- 审核邮件发送状态。

列表字段：审核单号、作品、作者、类型、包含章节数、字数、提交时间、等待时长、发布方式、计划发布时间、邮件状态、审核状态和当前审核员。

后台菜单需要显示待审核数量角标；该角标来自数据库待审记录数，不依赖邮件是否发送成功。

## 10.2 审核详情

作品首次投稿：

- 展示封面、标题、简介、语种、分类、类型和权利声明。
- 长篇展示本次提交的章节目录，可逐章预览。
- 短篇展示完整正文预览。
- 展示服务端字数和资源检测结果。
- 展示发布方式、作者时区、计划发布时间和“审核晚于计划时间将立即发布”的提示。
- 展示投稿提醒邮件发送状态、失败原因摘要和手动重发按钮。

已发布内容修改：

- 左侧展示当前公开版本。
- 右侧展示待审版本。
- 标题、简介、封面、分类和正文支持差异高亮。
- 不允许审核员直接编辑作者提交内容。

## 10.3 审核操作

- 领取/开始审核。
- 审核通过。
- 审核驳回。
- 驳回原因模板 + 自定义整改说明。
- 释放审核单。
- 查看历史审核记录。
- 管理员强制下架和恢复。
- 审核通过时严格遵守作者选择的发布方式；审核员不能无提示地把定时发布改成立即发布。

驳回必须填写对作者可见的具体原因，禁止只返回“审核失败”。

## 10.4 作品管理

- 查询所有作者作品。
- 查看公开资料、章节和历史修订。
- 查看阅读、点赞、收藏、评论和字数统计。
- 调整推荐状态和排序。
- 平台下架/恢复，并填写原因。
- 修正分类。
- 将已完结作品恢复为连载中。
- 查看作品相关审核单和操作日志。

后台直接修改公开内容不属于常规流程；确需紧急修正时必须产生管理员修订和操作日志。

## 10.5 分类与投稿规则配置

- 设置哪些分类组对创作者可见。
- 设置分类组是否必填、最少和最多选择数。
- 设置封面格式、尺寸和文件大小。
- 设置长篇单章和短篇正文长度限制。
- 设置首次长篇投稿最少章节数。
- 设置定时发布开关、最小提前量、扫描频率和失败重试策略。
- 设置投稿提醒收件邮箱、重试次数和后台审核链接前缀。
- 设置是否允许可信作者免人工复审；首期固定关闭。

## 10.6 后台权限

至少拆分：

- 作品只读。
- 投稿审核。
- 章节审核。
- 平台下架/恢复。
- 分类配置。
- 投稿规则配置。

菜单和接口都要校验权限，隐藏菜单不能替代后端权限校验。

---

## 11. 通知需求

## 11.1 站内消息

复用 `novel_message`，建议新增消息类型：

- `6=创作者审核通知`。
- `7=创作者作品管理通知`。

`content` JSON 至少包含：

- `novel_id`。
- `submission_id`。
- `submission_type`。
- `submission_status`。
- `chapter_revision_ids`（有章节时）。
- `review_note`。
- App 路由类型。

## 11.2 FCM

以下事件发送 FCM：

- 首次投稿通过或驳回。
- 作品资料修改通过或驳回。
- 章节通过或驳回。
- 完结申请结果。
- 平台下架/恢复。

推送发送失败时保留站内消息，并记录可重试日志。

## 11.3 读者更新通知

新章节公开后通知关注作者或收藏作品的读者属于 P1 能力：

- 批量发布章节时合并通知，避免连续推送多条。
- 修改旧章节不向读者发送“更新章节”通知。
- 作者草稿保存和提交审核不通知读者。

---

## 12. 文件与正文存储要求

- 本地 MySQL 保存元数据、状态、统计、资源索引和审核记录。
- 封面和正文继续使用对象存储/CDN，数据库只保存 URL 和资源信息。
- 不能把大型小说正文直接保存到 `text` 字段作为公开正文来源。
- 正文文件使用 UTF-8 纯文本。
- 对象路径必须使用不可猜测 ID，不包含作者邮箱、账号或原始文件名。
- 对象不能覆盖写；每次修订生成新 object key，审核通过只切换引用。
- 被驳回或废弃的资源按保留策略延迟清理，不能立即删除审核证据。
- 已发布历史版本保留，用于回滚；保留期限由后台策略确定。

---

## 13. 并发与数据一致性

### 13.1 多设备编辑

- 草稿保存请求携带 `lock_version`。
- 服务端只在版本匹配时更新，并把版本加一。
- 版本不匹配返回明确冲突码和服务端最近更新时间。
- Flutter 不得收到冲突后自动覆盖服务端内容。

### 13.2 章节编号

- 同一作品分配新章节号时锁定作品或语种版本记录。
- 使用现有唯一索引 `uk_novel_language_chapter_no` 作为最后保护。
- 发布失败必须完整回滚章节、计数和审核状态。
- 不允许依赖客户端传入的最大章节号加一。

### 13.3 缓存字段

每次公开内容变更后在事务中重新计算或增量维护：

- `novel_main_language.chapter_count`。
- `novel_main_language.word_count`。
- `novel_main.latest_chapter_no`。
- `novel_main.latest_update_time`。

需要提供后台修复脚本，可按 `novel_id` 从公开章节重新生成这些字段。

---

## 14. 安全与内容治理

- 每个写接口都执行作者身份和对象归属校验，防止横向越权。
- 每个后台审核接口都执行管理员角色权限校验。
- 正文和标题按纯文本处理，转义展示，防止 XSS。
- 上传文件按文件头和解码结果判断，不信任扩展名和 MIME 请求头。
- 封面、标题、简介和正文进入内容安全检查。
- 禁止任意 URL 写入资源字段，避免 SSRF 和读取非授权 CDN。
- 审核快照不可变，审核记录不可由作者删除。
- 公开作品只软删除，保留用户收藏、评论和阅读历史的关联完整性。
- 平台下架后正文接口立即拒绝访问并清除缓存。
- 作者被禁用后不能继续保存、提交和上传，但已公开内容是否下架由管理员决定。
- 权利声明需要记录版本号、勾选时间、用户 ID 和 IP，便于版权追溯。

---

## 15. 数据迁移方案

上线前先备份本地 `novel` 数据库，并使用可重复执行的迁移脚本，不直接手工改生产结构。

### 15.1 现有作品回填

- `publish_status = 4` → `work_type = 2`、`serialization_status = 2`、`public_status = 2`。
- `publish_status = 1` → `work_type = 1`、`serialization_status = 1`、`public_status = 2`。
- `publish_status = 2` → `work_type = 1`、`serialization_status = 2`、`public_status = 2`。
- `publish_status = 3` → `public_status = 4`；作品类型按是否存在公开章节/短篇正文辅助判断，无法确认的记录输出人工处理清单。
- 已公开现有作品的 `initial_audit_status = 3`。
- `first_publish_time` 无历史数据时可以使用 `create_time` 作为兼容值，但需要在迁移日志标明是推定值。
- `original_language_id` 不允许简单取最小语种 ID；无法确认时保留为空，后续由管理员补录。

### 15.2 历史版本基线

- 可为现有公开作品和章节生成一条“迁移基线修订”，状态为已发布。
- 基线修订的 `create_time` 使用迁移时间，同时保存原记录时间字段。
- 基线生成脚本必须可断点继续，并避免重复创建。

### 15.3 分类迁移

- 现有分类关系默认回填 `relation_type = 2`。
- 后台再批量指定主分类。
- 首页分类、榜单、热门搜索、短篇拉黑分类默认不得设置为作者可见。

### 15.4 灰度顺序

1. 先迁移表结构和历史数据。
2. 部署后端公开状态保护和兼容读取。
3. 部署后台审核功能。
4. 部署创作者写接口，但保持创作者投稿开关关闭。
5. 发布 Flutter 创作者中心。
6. 小范围创作者灰度。
7. 验证数据、缓存、审核和推送后全量开启。

---

## 16. 开发阶段拆分

## P0：可上线的投稿闭环

- 数据库迁移和历史数据回填。
- 创作者权限中间件。
- 受限资源上传。
- 长篇/短篇作品草稿。
- 长篇章节草稿。
- 服务端草稿列表、跨设备调阅和软删除。
- 自动保存和乐观锁。
- 作品和章节提交审核。
- 待审核邮件提醒、失败重试和后台手动重发。
- Vue 待审队列、详情、通过、驳回。
- 审核通过原子发布。
- 审核通过立即发布和审核通过定时发布。
- 可恢复、幂等的数据库定时发布任务。
- 现有读者接口公开状态保护。
- 正文缓存失效。
- Flutter 创作者中心、作品列表、作品编辑、章节编辑、短篇编辑和审核记录。
- `user_info` 作者态“小说数量、小说收藏数量、评论数量”统计卡片。
- 站内审核消息和 FCM。

## P1：作者体验完善

- 卷管理。
- 批量章节操作。
- 作者主动下架和重新上架申请。
- 关注作者/收藏作品的新章节通知。
- 作品每日数据趋势。
- 审核领取、超时和工作量统计。
- 更细的驳回原因模板。

## P2：商业化和高级能力

- 签约、稿费、结算、提现。
- 作家等级和任务体系。
- 推荐位申请。
- 抄袭和相似度检测。
- AI 辅助写作。
- 合著作者和编辑协作。
- 多语种翻译投稿工作流。
- 章节付费/VIP 运营策略。

---

## 17. 验收标准

## 17.1 长篇首次投稿

- 已认证创作者能创建长篇草稿、上传封面、选择分类并新增章节。
- 未认证用户调用任何创作者写接口都被拒绝。
- 草稿、待审作品不会出现在搜索、推荐、详情、章节和正文接口。
- 后台审核通过后，作品和首批章节同时对读者可见。
- `chapter_count`、`word_count`、`latest_chapter_no` 和时间字段正确。
- 审核驳回后读者不可见，作者能看到原因并继续修改。

## 17.2 长篇持续更新

- 作者能给自己的连载作品新增章节。
- 新章节审核通过前读者目录不显示。
- 发布并发请求不会生成重复章节号。
- 发布后目录和正文立即可读，Redis 不返回旧内容。
- 修改已发布章节待审时，读者仍读取旧版本。
- 修改通过后正文切换到新版本，`publish_time` 不变，`update_time` 更新。

## 17.3 短篇投稿

- 短篇能保存完整正文草稿，不生成章节记录。
- 审核通过后 `publish_status = 4`，进入短篇列表并可通过加密正文接口读取。
- 短篇修订待审时继续显示旧版本。
- 短篇正文切换后缓存立即失效。

## 17.4 权限与安全

- 作者 A 不能读取或修改作者 B 的草稿、资源、审核单和章节修订。
- 客户端伪造 `author_id`、`content_url`、章节号和字数无效。
- 任意公网正文 URL、超限文件和伪装文件上传被拒绝。
- 两名审核员同时操作同一审核单时只有一个请求成功。
- 平台下架后所有正文入口立即不可访问。

## 17.5 Flutter 质量

- 日间、夜间主题可用。
- 中文、英文、法文、斯瓦希里语界面无关键布局溢出。
- Android/iOS 字重符合项目规范。
- 自动保存失败、网络断开、Token 失效、版本冲突都有明确反馈。
- 长章节编辑和大章节列表滚动无明显卡顿。

## 17.6 回归测试

- 原有小说详情、长篇阅读、短篇阅读、搜索、推荐、榜单、收藏、点赞、评论、关注作者和阅读历史正常。
- 历史作品迁移后仍能正常阅读。
- 正文接口仍不向客户端泄露 CDN 原始 URL。

## 17.7 多作品与草稿

- 同一作者可以创建多本长篇和短篇，作品之间的草稿、章节、资源和审核状态互不串用。
- 作者退出并在另一台设备登录后，可以从“我的作品”继续调阅最近保存的服务端草稿。
- 草稿列表按最近编辑时间排序，并能筛选草稿、待审、待发布、驳回和已发布状态。
- 删除草稿采用软删除，不能误删同作者的其他作品或已发布内容。

## 17.8 定时发布

- 作者能为首次作品、短篇修订、新章节和章节修订选择未来发布时间。
- 审核提前通过时，内容在指定时间前对读者不可见。
- 审核晚于指定时间时，按确认规则在审核通过后立即发布。
- PM2 重启、服务短时中断或任务重复执行都不会漏发或重复发布。
- 作者只调整待发布的未来时间时不重复审核正文，但操作有完整日志。
- 修改待发布正文时必须重新审核。
- 定时发布实际执行时间与计划时间误差不超过第一阶段约定值。

## 17.9 待审核邮件

- 每个新审核单向 `2209197172@qq.com` 发送且只发送一封提醒邮件。
- 批量章节审核单只发送一封汇总邮件。
- 邮件包含审核单号、作者、作品、发布方式、计划时间和后台审核链接。
- 邮件不包含正文内部 URL、Token、密码或完整正文。
- SMTP 暂时不可用时投稿仍成功出现在后台待审列表，并能自动重试或由管理员手动重发。

## 17.10 `user_info` 作者统计

- 普通用户继续看到原有评论、点赞、收藏消息统计。
- `author == 2` 时显示小说数量、小说收藏数量和评论数量。
- 小说数量包含服务端草稿，点击后进入当前作者的完整作品列表。
- 收藏和评论主数值与作者名下作品实际聚合结果一致。
- 作者不能通过传入其他 `author_id` 查询他人的创作统计。

---

## 18. 测试要求

### 后端自动化测试

- 权限和归属测试。
- 长篇/短篇字段组合校验。
- 草稿乐观锁冲突测试。
- 重复提交幂等测试。
- 审核并发测试。
- 章节号并发分配测试。
- 发布事务回滚测试。
- 定时发布到期、逾期审核、重复执行、失败重试和服务重启恢复测试。
- 投稿邮件幂等、发送失败重试和手动重发测试。
- 作者多作品隔离和三项聚合统计测试。
- 字数计算多语种测试。
- 资源归属和 MIME 检测测试。
- 缓存失效测试。
- 历史数据迁移和重复执行测试。

### Flutter 测试

- 创作者状态分流 Widget 测试。
- 新建作品分步表单测试。
- 自动保存、冲突、离线恢复测试。
- 多作品列表和跨设备服务端草稿恢复测试。
- 长篇/短篇条件展示测试。
- 立即发布/定时发布选择、时区展示和待发布状态测试。
- 普通用户/作者统计卡片分流与跳转测试。
- 审核状态和通知跳转测试。
- 日间/夜间及多语种 Golden/Widget 测试。

### 后台测试

- 筛选和分页测试。
- 审核详情版本对比测试。
- 待审列表发布方式、计划时间、邮件状态和重发操作测试。
- 权限菜单和接口双重校验测试。
- 重复审核和非法状态流转测试。

---

## 19. 开发规范

### Flutter

- 遵守 `/Users/ali/work/小说/app/开发规范.md`。
- 页面 UI 放 `index.dart`，交互逻辑放 `logic.dart`，样式放 `style.dart`，复杂组件放 `widgets`。
- 文件和目录使用小写下划线。
- 可复用的上传、编辑器状态、自动保存、审核标签和错误提示抽成公共组件或公共逻辑。
- 所有颜色从公共颜色配置获取，所有尺寸集中管理。
- 所有用户文案使用多语言资源。
- 使用 Flutter 命令时执行：`cd /Users/ali/work/小说/app && fvm flutter ...`。

### Node.js 后端

- 遵守 `/Users/ali/work/小说/后端/后端开发规范.md`。
- 使用 ES6 module。
- 一个文件尽量只实现一个接口或一个公共函数。
- SQL 使用参数化查询。
- 发布、审核、计数更新必须使用事务。
- 状态值、长度限制、文件限制和消息类型集中到配置文件，不使用魔法数字。
- 中文注释按项目规范使用 `TODO` 前缀。
- 数据库迁移脚本放入统一迁移目录，并提供执行结果校验和回滚说明。

### Vue 后台

- 页面、逻辑、样式和 API 分文件维护。
- 审核操作必须有二次确认、加载状态和防重复提交。
- 列表全部服务端分页。
- 正文预览做虚拟化或按需加载，禁止一次渲染超大批量章节正文。
- 菜单权限与后端接口权限保持一致。

---

## 20. 开发前必须确认的产品参数

### 20.1 本轮已确认

- 第一阶段不做 AI 辅助写作。
- 一个作者可以发布和管理多本小说。
- 草稿保存到服务端，作者可以随时调阅。
- 我的作品列表包含草稿。
- 保留审核流程和后台待审核列表。
- 每个审核单进入待审后提醒 `2209197172@qq.com`。
- 支持审核通过立即自动发布。
- 支持审核通过按指定时间自动发布。
- `user_info` 作者态替换为小说数量、小说收藏数量和评论数量。
- 点击小说数量进入包含草稿的作者作品列表。

### 20.2 建议按默认方案确认

以下问题已经给出推荐答案，正式编码前只需要确认是否接受：

1. “小说数量”是否按所有未删除作品统计，包含草稿、待审、驳回、待发布、已发布和已下架？推荐：是。
2. “小说收藏数量”和“评论数量”是否按作者全部已公开过作品当前 `favorite_count`、`comment_count` 求和，其中评论包含顶层评论和回复？推荐：是，直接沿用现有作品统计口径。
3. 点击收藏和评论统计是否进入对应消息筛选页？推荐：是；如需作品数据排行则另做页面。
4. 定时发布是否同时适用于首次作品、短篇更新、新章节和章节修订？推荐：全部适用。
5. 审核完成时计划时间已经过去，是否立即发布？推荐：立即发布。
6. 第一阶段是否只提供“审核通过立即发布”和“审核通过定时发布”两种模式，不做审核通过后手动发布？推荐：只做两种。
7. 邮件发送失败时投稿是否仍然成功进入后台待审队列，由系统自动重试？推荐：是，避免 SMTP 故障导致稿件丢失。
8. 是否所有首次作品、资料更新、短篇正文、新章节和章节修改都要审核？推荐：是；第一阶段不接机器免审。
9. 审核通过待发布期间，是否允许只修改计划时间而不重新审核正文？推荐：允许；正文发生任何变化必须重新审核。

### 20.3 仍需配置的数值

以下数值不影响总体方案，未单独指定时采用本文建议默认值并做成后台配置：

1. 首次长篇投稿最少章节数。
2. 单章最少/最多字数。
3. 短篇最少/最多字数。
4. 作品标题、简介最终长度。
5. 作者可选的主分类和标签数量。
6. 封面最低尺寸和最大文件大小。
7. 定时发布最小提前量和允许调度误差。
8. 单个作者最多可保留的活跃草稿/作品数。
9. 驳回版本和未引用资源的保留时间。
10. 是否允许作者立即下架自己的已发布作品。

所有数值由后端配置接口下发，不得硬编码到 Flutter 页面。

---

## 21. 最终实施结论

- 不需要重做已经完成的邮箱验证码和创作者认证流程。
- 需要新增 5 张核心表：作品修订、章节修订、审核单、审核单明细、创作者资源。
- 建议给 `novel_main` 增加作品类型、连载状态、公开状态和首次审核状态，保留现有 `publish_status` 兼容读者端。
- 现有小说、语种、章节和分类表继续作为审核通过后的公开数据。
- Flutter 需要从占位作者页扩展为完整创作者工作台。
- Flutter `user_info` 需要按作者身份切换三项统计，并从小说数量进入包含草稿的我的作品列表。
- Node 后端需要新增创作者写入、服务端草稿、资源上传、审核发布、定时发布、审核邮件、权限、幂等、缓存失效和通知能力。
- Vue 后台需要新增作品/章节待审核列表、发布计划、邮件状态和作品管理模块。
- 开发顺序必须先完成数据库和读者接口隔离，再开放创作者写入，避免草稿或待审正文泄露。
