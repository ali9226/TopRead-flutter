import 'package:app/config/layout_config.dart';

/// 主列表区块样式常量
class MainListStyle {
  /// 页面横向边距
  static const double page_horizontal_padding =
      LayoutConfig.page_horizontal_padding;

  /// 顶部基础内边距
  static const double top_padding = 0;

  /// 底部阅读进度遮罩高度（用于底部 padding）
  static const double reading_mask_height = 96;

  /// 顶部封面距离状态栏的间距
  static const double cover_top_spacing = -20;
}
