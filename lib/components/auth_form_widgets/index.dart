/// 认证表单公共子组件。
///
/// 提供登录/注册页面复用的 UI 模块，包括：
/// - 品牌区（Logo + 标语）
/// - 顶部栏（渐变遮罩 + 语种切换）
/// - 账号状态提示（已注册/未注册）
/// - 邀请码输入模块（带过渡动画）
/// - 提交按钮（根据模式显示登录/注册）
library;

export 'auth_brand_section.dart';
export 'auth_top_bar.dart';
export 'auth_account_status_hint.dart';
export 'auth_invitation_code_section.dart';
export 'auth_submit_button.dart';
