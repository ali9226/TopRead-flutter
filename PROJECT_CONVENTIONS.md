/* TODO 项目命名与注释规范

# 1. 适用范围

- 本规范适用于 `/app` 下所有人工维护的源码、资源说明文档和工程配置文件。
- Flutter / Gradle / CocoaPods / FVM 自动生成目录只做审查，不直接写入说明文件，也不主动改名。

# 2. 目录与文件命名

- 目录名统一使用 `snake_case`。
- Dart 文件名统一使用 `snake_case.dart`。
- Markdown 说明文件统一使用 `README.md`。
- 资源文件优先使用小写英文；多单词使用下划线分隔。

# 3. Dart 标识符命名

- 类名、枚举名、扩展名按 Dart 约定使用 `PascalCase`。
- 变量名、函数名、参数名按 Dart 约定优先使用 `lowerCamelCase`。
- 常量名优先使用 `lowerCamelCase`；仅在已有历史约束或跨语言协议要求时保留特殊命名。
- 路由路径统一使用小写英文和下划线，例如 `/top_up_record`。

# 4. 页面与组件目录规范

- 路由页面目录默认使用三段式结构：
  - `index.dart`：只放页面 UI 结构和子组件组合。
  - `logic.dart`：只放交互逻辑、状态处理、数据请求。
  - `style.dart`：只放页面级样式常量和视觉配置。
- 子组件目录也遵循相同思想：
  - UI 放 `index.dart`
  - 逻辑放 `logic.dart`
  - 样式放 `style.dart`

# 5. 注释规范

- 所有代码注释前缀统一使用 `TODO`，方便 IDE 高亮检索。
- 单行注释统一写法：`// TODO 注释内容`
- 多行注释统一写法：

```dart
/* TODO
 * 注释内容第一行
 * 注释内容第二行
 */
```

- 组件构造参数必须说明参数作用、默认值和使用场景。
- 路由页面文件顶部必须说明路由名称和页面职责。
- 复杂函数必须说明：
  - 函数用途
  - 参数意义
  - 返回值语义
  - 关键流程

# 6. 目录说明文件规范

- 每个人工维护目录应提供一个 `README.md`。
- `README.md` 至少说明：
  - 当前目录的用途
  - 主要文件或子目录职责
  - 与其它目录的协作关系
  - 禁止放入的内容

# 7. 审查处理原则

- 未使用的参数、导入、样式、组件和代码应删除。
- 重复 UI、重复逻辑、重复样式优先抽成公共组件或公共方法。
- 生成物、缓存、第三方产物目录不做手工改名，但要在审查记录中标注为“生成物”。
- 重构后必须执行格式化和静态检查。
*/
