# 长篇小说阅读页面重构需求文档

> 本文档详细描述 `lib/pages/read` 文件夹下所有代码的完整需求
> 参考开发规范：`./开发规范.md`

---

## 一、页面概述

阅读页面是长篇小说的核心阅读界面，主要功能包括：
1. 小说详情展示（封面、作者、统计、标签、简介、评论）
2. 章节正文阅读（支持无限滚动加载上下章）
3. 阅读进度保存与恢复
4. 导航栏（顶部/底部）显示与隐藏
5. 目录跳转
6. 阅读设置（字号、主题）
7. 自动阅读功能
8. 点赞、收藏、评论、分享功能

---

## 二、目录结构

```
lib/pages/read/
├── index.dart          # 主页面 UI（StatefulWidget，约1343行，需要拆分）
├── logic.dart          # 逻辑层（GetxController，约1329行，需要拆分）
├── style.dart          # 页面级样式常量
├── utils/
│   └── scroll_utils.dart    # 滚动工具类
└── widgets/
    ├── auto_read_settings_button/   # 自动阅读设置浮动按钮
    ├── auto_read_settings_sheet/    # 自动阅读设置弹窗
    ├── back_button/                 # 返回按钮（未使用？）
    ├── background_glow/             # 背景光斑装饰
    ├── content/                     # 正文内容组件
    ├── introduction_section/        # 介绍区组件（封面、作者、统计、标签、简介、评论）
    ├── main_list/                   # 主滚动列表
    ├── navigation_bars/             # 导航栏
    │   ├── top/                     # 顶部导航栏
    │   └── bottom/                  # 底部导航栏
    ├── read_directory_sheet/        # 目录弹窗
    ├── reading_progress_mask/       # 阅读进度遮罩
    ├── reading_progress_text/       # 阅读进度文字
    ├── reading_settings_sheet/      # 阅读设置弹窗
    ├── skeleton/                    # 骨架屏
    └── start_reading_pill/          # "上滑开始阅读"胶囊
```

---

## 三、详细功能需求

### 3.1 主页面（index.dart）

#### 3.1.1 页面参数
- `story_id` (int): 书籍ID，必传
- `story_title` (String): 书籍标题，必传
- `initial_comment_id` (int): 评论ID，可选，用于从消息页跳转时自动打开评论区

#### 3.1.2 状态变量
```dart
Logic logic;                           // 逻辑层实例
DeviceInfo device_info;                // 设备信息仓库
NovelReadingStore reading_store;       // 阅读内容仓库
GlobalKey reading_section_key;         // 正文区块定位锚点
bool has_started_reading = false;      // 是否已进入正文阅读状态
bool _is_restoring_progress = false;   // 是否正在恢复阅读进度
double _cached_chapter_progress = 0;   // 缓存的章节内进度百分比
double reading_progress_percent = 0;   // 当前滚动进度百分比
double reading_progress_ratio = 0;     // 当前滚动进度比例
double last_viewport_dimension = 0;    // 最近视口高度（判断横竖屏切换）
double pending_restore_ratio = 0;      // 切换屏幕方向前的滚动比例
bool needs_restore_scroll_position = false;  // 是否需要恢复滚动位置
bool is_reading_section_at_top = false;      // 正文是否贴近顶部
DateTime _last_chapter_switch_time;          // 最近章节切换时间戳（冷却用）
Ticker? _auto_read_ticker;                   // 自动阅读 Ticker
Duration? _auto_read_last_tick;              // 自动阅读上一帧时间戳
bool _is_programmatic_scroll = false;        // 是否正在程序化滚动
bool _is_scroll_activity_active = false;     // 是否存在滚动活动
Timer? _progress_save_timer;                 // 进度保存定时器
bool _has_user_engaged = false;              // 用户是否有真实阅读行为
```

#### 3.1.3 初始化流程
1. 注册 `WidgetsBindingObserver` 监听屏幕尺寸变化
2. 使用 `Get.put` 初始化 `Logic`，tag 为 `story_id`（确保 State 重建时复用实例）
3. 设置 `can_apply_chapter_mutation` 回调（控制是否允许修改正文列表）
4. 路由参数非法时回退首页
5. 注册滚动监听 `_handle_scroll`
6. 调用 `_init_with_progress_restore()` 初始化（带进度恢复）
7. 如果有 `initial_comment_id`，自动打开评论区
8. 检查是否有需要恢复的滚动位置（主题切换后）
9. 监听错误状态，失败时自动返回

#### 3.1.4 核心方法

##### `_init_with_progress_restore()`
初始化页面，有阅读进度时静默加载目标章节。
- 流程：查询阅读记录 → 设置静默模式 → 加载详情 → 跳转目标章节 → 等待布局 → 滚动到保存位置 → 退出静默模式
- 启动定时保存

##### `_handle_scroll()`
滚动监听核心方法，负责：
1. 更新 `has_started_reading` 状态（第一章标题是否滑过底部阈值）
2. 计算 `reading_progress_percent`（基于字数的进度算法）
3. 计算 `reading_progress_ratio`
4. 更新 `is_reading_section_at_top` 状态
5. 检测是否需要加载上一章/下一章（预取逻辑）
6. 实时更新当前阅读章节索引（通过查找视口中最近的章节标题）
7. 确保下一章已拼接

##### `_scroll_to_reading_section()`
点击"上滑开始阅读"胶囊后，滚动到正文阅读位置。

##### `_scroll_to_chapter(int index)`
滚动到指定章节位置。

##### `_showSettingsSheet()`
显示阅读设置弹窗（字号调节、主题切换、自动阅读）。

##### `_start_auto_read()` / `_stop_auto_read()`
开始/停止自动阅读。

##### `_open_comment_sheet()`
打开评论弹窗。

##### `_save_current_progress()` / `_save_current_progress_on_exit()`
保存阅读进度（定时/退出时）。

#### 3.1.5 UI 构建（build 方法）
页面采用 `Obx` 响应式构建，主要结构：
```
AnimatedContainer (背景色)
└── Scaffold
    └── Stack
        ├── ReadBackgroundGlow (背景光斑)
        ├── NotificationListener + RefreshIndicator + ReadMainList (主滚动列表)
        ├── PageTopGradientOverlay (顶部渐变遮罩)
        ├── 骨架屏覆盖层 (目录跳转时)
        ├── ReadingProgressMask (底部阅读进度遮罩)
        ├── ReadingProgressText (底部进度文字)
        ├── ReadStartReadingPill (上滑开始阅读胶囊)
        ├── ReadTopBar (顶部导航栏)
        ├── ReadBottomBar (底部导航栏)
        ├── 切换章节加载遮罩
        └── AutoReadSettingsButton (自动阅读设置按钮)
```

---

### 3.2 逻辑层（logic.dart）

#### 3.2.1 数据模型

##### ReadDetail
```dart
class ReadDetail {
  int story_id;              // 书籍ID
  String title;              // 标题
  String cover_url;          // 封面地址
  int author_id;             // 作者ID
  String author_avatar_url;  // 作者头像
  String author_name;        // 作者名称
  bool focus_on;             // 是否关注作者
  String score_major_text;   // 评分整数部分
  String score_minor_text;   // 评分单位文案
  String review_count_text;  // 点评人数文案
  String reading_major_text; // 在读人数整数部分
  String reading_minor_text; // 在读人数单位文案
  String reading_subtitle_text;  // 在读副标题文案
  String word_count_major_text;  // 字数整数部分
  String word_count_minor_text;  // 字数单位文案
  String word_count_subtitle_text;  // 字数副标题文案
  List<String> tag_list;     // 标签列表
  String intro_text;         // 简介内容
  String chapter_title;      // 第一章标题
  List<ReadComment> comment_list;  // 热门评论列表
}
```

##### ReadComment
```dart
class ReadComment {
  String avatar_url;   // 评论人头像
  String user_name;    // 评论人名称
  String content;      // 评论内容
  int star_count;      // 评论星级
}
```

#### 3.2.2 状态变量
```dart
// 加载状态
var is_loading = true.obs;
bool is_loading_next = false;
var is_switching_chapter = false.obs;
var is_jumping_chapter = false.obs;
DateTime jump_finished_time;
var is_error = false.obs;

// 章节状态
var current_chapter_index = 0.obs;
int current_chapter_db_id = 0;
int _loaded_chapter_index = 0;      // 已加载的最大章节索引
int _min_loaded_chapter_index = 0;  // 已加载的最小章节索引
bool is_loading_prev = false;
DateTime _last_load_prev_time;      // 上次加载上一章时间
DateTime _last_load_next_time;      // 上次加载下一章时间

// 交互状态
var show_navigation = false.obs;
var is_liked = false.obs;
var like_count = 0.obs;
var is_like_loading = false.obs;
var is_favorited = false.obs;
var is_auto_reading = false.obs;

// 设置
late RxDouble body_font_size;      // 正文字号（16.0-36.0）
late RxDouble auto_read_speed;     // 自动阅读速度（0.0-1.0）

// 滚动方向检测
double _last_scroll_offset = 0;
bool _is_scrolling_down = false;
double _scroll_offset_when_visible = 0;

// 章节缓存
Map<int, GlobalKey> _chapter_keys = {};  // 章节锚点key映射
```

#### 3.2.3 核心方法

##### `fetch_info()`
请求书籍详情接口。
- 参数：`force`（强制刷新）、`show_loading`（显示加载态）、`bypass_chapter_cache`（绕过章节缓存）
- 流程：请求详情 → 保存到 Store → 获取章节目录 → 获取第一章内容 → 预加载第二章

##### `fetch_directory()`
请求章节目录接口。

##### `load_next_chapter()` / `load_prev_chapter()`
加载下一章/上一章内容。
- 优先从缓存读取，缓存未命中则网络请求
- 等待滚动空闲后再修改列表（避免打断手势）
- 上一章插入后需要修正滚动偏移（无缝向上加载）

##### `jump_to_chapter(int index)`
跳转到指定章节。
- 清空 reading_items，仅显示目标章节
- 跳转后预加载前后各2章

##### `_fetch_chapter_content(int index)`
获取章节内容（三层缓存）：
1. 内存缓存（Store）
2. 磁盘缓存（7天有效期）
3. 网络请求

##### `calculate_reading_progress()`
根据滚动位置计算阅读百分比（基于字数算法）。

##### `calculate_chapter_progress_percent()`
根据全书阅读进度换算指定章节内部的阅读百分比。

##### `find_chapter_index_by_progress()`
根据全书阅读百分比推算所在章节索引。

##### `on_scroll(double offset)`
滚动方向检测，自动显示/隐藏导航栏。
- 上滑（内容向上移动）时隐藏，下滑时显示
- 8px 阈值防止误触
- 接近顶部 300px 以内时，上滑不显示导航栏

##### `toggle_like()` / `toggle_favorite()`
切换点赞/收藏状态。

##### `build_detail()`
构建 ReadDetail 数据模型。

##### `build_reading_items()`
构建正文内容项列表。

---

### 3.3 样式层（style.dart）

```dart
class Style {
  // 滚动交互配置
  static const int page_scroll_animation_duration_ms = 320;
  static const double page_scroll_step_offset = 100;
  static const double scroll_offset_epsilon = 1;
  
  // 胶囊触发距离
  static const double first_chapter_title_bottom_trigger_distance = 20;
  static const double chapter_title_top_target_offset = 20;
  
  // 下拉刷新
  static const double refresh_indicator_displacement = 54;
  
  // 章节预取阈值
  static const double load_next_chapter_threshold = 2400;
  static const double load_prev_chapter_threshold = 2400;
  static const double chapter_prefetch_viewport_multiplier = 2.2;
  
  // 正文区块"已到达顶部"容差
  static const double reading_section_at_top_threshold = 50;
  
  // 动画时长
  static const int progress_mask_animation_duration_ms = 220;
  static const int scroll_to_reading_section_duration_ms = 420;
  
  // 颜色配置
  static const Color light_background_color = Colors.white;
  static final Color dark_background_color = ColorConstants.nightBackgroundColor;
  static const Color light_navigation_bar_color = Colors.white;
  static const Color dark_navigation_bar_color = Color(0xFF1B2231);
  static final Color progress_bar_active_color = ColorConstants.themeColor;
  static const Color progress_bar_gradient_end_color = Color(0xFFFF9E80);
}
```

---

### 3.4 工具类（utils/scroll_utils.dart）

```dart
class ScrollUtils {
  /// 判断正文区域是否已经滚动到屏幕顶部附近
  static bool is_reading_section_at_top({
    required GlobalKey? reading_section_key,
    required BuildContext context,
  });
  
  /// 向上滚动接近一屏，预留底部 50 像素
  static Future<void> scroll_page_up({
    required ScrollController scroll_controller,
    required BuildContext context,
  });
  
  /// 向下滚动接近一屏，预留底部 50 像素
  static Future<void> scroll_page_down({
    required ScrollController scroll_controller,
    required BuildContext context,
  });
}
```

---

### 3.5 子组件需求

#### 3.5.1 ReadMainList（主滚动列表）
- 文件：`widgets/main_list/index.dart`
- 功能：将封面信息 + 热门书评 + 正文阅读组织成一个可滚动主列表
- 参数：logic, scroll_controller, status_bar_height, is_dark, detail, reading_items, reading_section_key, is_reading_section_at_top, on_page_up, on_page_down, on_middle_tap, show_navigation

#### 3.5.2 ReadContent（正文内容组件）
- 文件：`widgets/content/index.dart`
- 功能：展示小说正文内容，包括标题、提示信息以及逐段渲染的正文文本
- 支持点击分区翻页（上1/3上翻、中1/3显示/隐藏导航栏、下1/3下翻）
- 参数：logic, is_dark, detail, reading_items, reading_section_key, is_reading_section_at_top, on_page_up, on_page_down, on_middle_tap, show_navigation

#### 3.5.3 ReadIntroductionSection（介绍区组件）
- 文件：`widgets/introduction_section/index.dart`
- 功能：展示小说完整介绍信息
- 子组件：
  - ReadAuthorSection（作者信息：头像、名称、关注）
  - ReadStatSection（统计：评分、在读人数、字数）
  - ReadTagSection（标签列表）
  - ReadIntroSummarySection（简介摘要）
  - ReadCommentSection（热门评论列表）
    - ReadCommentItem（单条评论）

#### 3.5.4 ReadTopBar（顶部导航栏）
- 文件：`widgets/navigation_bars/top/index.dart`
- 功能：返回按钮 + 收藏按钮 + 分享按钮
- 支持加载状态骨架屏
- 参数：is_dark, show, on_back, on_favorite_tap, on_share, is_favorited, is_loading

#### 3.5.5 ReadBottomBar（底部导航栏）
- 文件：`widgets/navigation_bars/bottom/index.dart`
- 功能：
  - 进度条区域：上一章 + 可拖动进度条 + 下一章
  - 四个操作项：目录、设置、评论、喜欢
- 参数：is_dark, show, progress, chapter_list, on_progress_changed_end, on_prev_chapter, on_next_chapter, is_first_chapter, is_last_chapter, on_catalog_tap, on_setting_tap, on_comment_tap, comment_count, is_liked, like_count, is_like_loading, on_like_tap

#### 3.5.6 ReadDirectorySheet（目录弹窗）
- 文件：`widgets/read_directory_sheet/index.dart`
- 功能：
  - 三个 Tab：详情、目录、笔记
  - 详情 Tab：显示书籍详情头部 + 简介
  - 目录 Tab：章节列表，支持滚动到当前章节，显示当前章节进度百分比
  - 笔记 Tab：暂无内容
- 子组件：BookDetailHeader, BookIntroduction

#### 3.5.7 ReadSettingsSheet（阅读设置弹窗）
- 文件：`widgets/reading_settings_sheet/index.dart`
- 功能：
  - 字号调节（增加/减少，带滚动动画）
  - 主题切换（日间/夜间，带遮罩层图标过渡效果）
  - 自动阅读入口
- 参数：body_font_size, font_size_min, font_size_max, on_increase_font_size, on_decrease_font_size, on_close, on_auto_read

#### 3.5.8 AutoReadSettingsSheet（自动阅读设置弹窗）
- 文件：`widgets/auto_read_settings_sheet/index.dart`
- 功能：
  - 速度调节滑块（慢 ↔ 快，拖动时显示百分比吐司）
  - 退出自动阅读按钮
- 参数：auto_read_speed, on_close, on_exit

#### 3.5.9 AutoReadSettingsButton（自动阅读设置浮动按钮）
- 文件：`widgets/auto_read_settings_button/index.dart`
- 功能：自动阅读时显示在底部居中的设置按钮
- 参数：is_dark, on_tap

#### 3.5.10 ReadStartReadingPill（上滑开始阅读胶囊）
- 文件：`widgets/start_reading_pill/index.dart`
- 功能：未进入正文阅读状态时，提示用户上滑或点击开始阅读
- 参数：is_dark, show_start_reading_pill, bottom_pill_background_color, bottom_safe_area, on_tap

#### 3.5.11 ReadingProgressMask（阅读进度遮罩）
- 文件：`widgets/reading_progress_mask/index.dart`
- 功能：底部阅读渐变遮罩，提供正文与底部进度区的视觉过渡

#### 3.5.12 ReadingProgressText（阅读进度文字）
- 文件：`widgets/reading_progress_text/index.dart`
- 功能：底部显示当前百分比进度

#### 3.5.13 ReadBackgroundGlow（背景光斑）
- 文件：`widgets/background_glow/index.dart`
- 功能：绘制两层光斑装饰

#### 3.5.14 ReadSkeleton / ReadContentSkeleton（骨架屏）
- 文件：`widgets/skeleton/index.dart`, `widgets/skeleton/content_skeleton.dart`
- 功能：加载中展示骨架屏

---

## 四、需要拆分的问题

### 4.1 index.dart 拆分建议（当前1343行）

1. **进度恢复逻辑** → `utils/progress_restore_utils.dart`
   - `_init_with_progress_restore()`
   - `_restore_last_read_position()`
   - `_restore_scroll_position_after_layout()`

2. **自动阅读逻辑** → `utils/auto_read_utils.dart`
   - `_start_auto_read()`
   - `_stop_auto_read()`
   - `_begin_auto_read_ticker()`

3. **章节跳转逻辑** → `utils/chapter_navigation_utils.dart`
   - `_scroll_to_chapter()`
   - `_scroll_to_reading_section()`
   - `_scroll_to_chapter_title_with_top_offset()`
   - `_wait_for_chapter_title_context()`
   - `_get_chapter_title_global_top()`
   - `_get_chapter_title_target_scroll_offset()`

4. **滚动处理逻辑** → `utils/scroll_handler.dart`
   - `_handle_scroll()`
   - `_handle_main_scroll_notification()`
   - `_is_first_chapter_title_past_start_threshold()`

5. **进度保存逻辑** → `utils/progress_save_utils.dart`
   - `_save_current_progress()`
   - `_save_current_progress_on_exit()`
   - `_start_progress_save_timer()`

6. **弹窗显示逻辑** → `utils/sheet_utils.dart`
   - `_showSettingsSheet()`
   - `_on_auto_read_settings_tap()`
   - `_open_comment_sheet()`

### 4.2 logic.dart 拆分建议（当前1329行）

1. **数据模型** → `models/read_detail.dart`
   - `ReadDetail` 类
   - `ReadComment` 类

2. **章节缓存逻辑** → `utils/chapter_cache_utils.dart`
   - `_chapter_cache_directory`
   - `_chapter_cache_file_name()`
   - `_chapter_cache_file()`
   - `_read_chapter_content_from_disk_cache()`
   - `_write_chapter_content_to_disk_cache()`
   - `_fetch_chapter_content()`
   - `_preload_adjacent_chapters()`
   - `_preload_chapter_window()`

3. **进度计算逻辑** → `utils/progress_calc_utils.dart`
   - `calculate_reading_progress()`
   - `calculate_chapter_progress_percent()`
   - `find_chapter_index_by_progress()`

4. **状态同步逻辑** → `utils/state_sync_utils.dart`
   - `sync_like_state()`
   - `update_focus_on()`
   - `update_comment_count()`

---

## 五、开发规范要求

### 5.1 命名规范
- 所有变量、文件、文件夹、函数等命名统一用小写+下划线

### 5.2 组件拆分
- 能抽离子组件处理的内容一定抽离给子组件处理
- 能复用的代码尽可能复用

### 5.3 样式管理
- 尽量不用魔法数字硬编码，全部用变量放在 `style.dart` 文件内统一管理
- 子组件内也可以有自己的 `style.dart` 文件

### 5.4 文件职责
- `index.dart`: 负责处理 UI
- `logic.dart`: 负责处理逻辑交互
- `style.dart`: 负责处理文字、方块等样式
- `widgets/`: 负责存放子组件代码

### 5.5 注释要求
- 注释尽可能详细，写清楚每一行代码的注释
- 每一个函数写清楚作用、参数用途、变量用途

### 5.6 函数拆分
- 尽量每一个函数都单独设置一个文件去存放

### 5.7 主题适配
- 所有 UI 的更改都要适配日间主题和夜间主题

### 5.8 多语种适配
- 所有涉及文字展示的 UI 组件，必须同时提供 CJK 和非 CJK 两套样式参数
- 使用 `LanguageUtil.is_cjk_language(languageCode)` 判断当前语种

### 5.9 字体字重规范
- 所有 FontWeight 必须用 `FontConfig.adjustedWeight()` 包裹
- 包含 `adjustedWeight` 的 TextStyle 不能使用 `const` 关键字
- 包含 `adjustedWeight` 的 StrutStyle 也不能使用 `const`

---

## 六、API 依赖

### 6.1 网络请求
- `get_last_read_record(novel_id)`: 查询最后阅读记录
- `save_read_progress(...)`: 保存阅读进度
- `postRequest<NovelInfo>(path: 'novel/get_info', ...)`: 请求小说详情
- `postRequest<List<NovelChapterInfo>>(path: 'novel_chapter/inquire', ...)`: 请求章节目录
- `get_chapter_content(content_url)`: 获取章节正文
- `postRequest<Map>(path: 'novel_like/click', ...)`: 点赞
- `postRequest<Map>(path: 'novel_favorite/click', ...)`: 收藏

### 6.2 Store 依赖
- `NovelReadingStore`: 小说阅读内容仓库
  - `novel_info`: 小说信息
  - `chapter_list`: 章节列表
  - `reading_items`: 正文内容项列表
  - `clear_novel_info()`: 清空小说信息
  - `set_novel_info()`: 设置小说信息
  - `set_chapter_list()`: 设置章节列表
  - `set_initial_content()`: 设置初始内容
  - `append_chapter_content()`: 追加章节内容
  - `prepend_chapter_content()`: 插入章节内容到头部
  - `cache_chapter_content()`: 缓存章节内容
  - `get_cached_chapter_content()`: 获取缓存的章节内容
  - `clear_chapter_content_cache()`: 清空章节内容缓存
  - `needs_restore_scroll_position`: 是否需要恢复滚动位置
  - `last_reading_progress_ratio`: 上次阅读进度比例
  - `last_scroll_offset`: 上次滚动偏移

- `DeviceInfo`: 设备信息仓库
  - `theme`: 当前主题模式

### 6.3 工具依赖
- `LanguageUtil.is_cjk_language()`: 判断是否为 CJK 语种
- `FontConfig.adjustedWeight()`: 字体字重适配
- `routerUtil()`: 路由工具
- `showBottomTip()`: 底部提示
- `showLoginRequiredDialog()`: 登录弹窗
- `showCommentSheet()`: 评论弹窗
- `showShareSheet()`: 分享弹窗

---

## 七、关键业务逻辑

### 7.1 阅读进度恢复流程
1. 进入页面时查询阅读记录
2. 有进度 → 设置静默模式，显示"正在恢复进度"提示
3. 加载小说详情和章节目录
4. 根据 chapter_id 或 read_progress 找到目标章节
5. 调用 jump_to_chapter 跳转到目标章节
6. 等待布局完成
7. 根据 chapter_offset 或 read_progress 滚动到保存位置
8. 退出静默模式

### 7.2 章节无限滚动加载
1. 滚动监听检测是否接近底部/顶部
2. 预取距离 = max(固定阈值, 视口高度 × 倍数)
3. 接近底部时加载下一章，接近顶部时加载上一章
4. 等待滚动空闲后再修改列表（避免打断手势）
5. 上一章插入后修正滚动偏移（无缝向上加载）

### 7.3 自动阅读功能
1. 使用 Ticker 驱动，每帧根据速度滚动
2. 速度映射：0.0 → 20px/s，1.0 → 300px/s
3. 滚动到底部时自动停止
4. 支持速度调节（0.0-1.0）

### 7.4 导航栏显隐逻辑
1. 上滑（内容向上移动）时隐藏导航栏
2. 下滑（内容向下移动）时显示导航栏
3. 8px 阈值防止误触
4. 接近顶部 300px 以内时，上滑不显示导航栏
5. 点击正文中间区域切换导航栏显示状态

### 7.5 点击分区翻页
- 上 1/3 区域：上翻一屏
- 中 1/3 区域：显示/隐藏导航栏
- 下 1/3 区域：下翻一屏
- 正文未贴顶时禁用点击翻页

---

## 八、注意事项

1. **Get.put + tag**: Logic 使用 tag 持久化，确保 State 重建（如主题切换）时能找回原有实例
2. **滚动空闲检测**: 章节拼接必须等待滚动 idle，否则会打断手势导致卡顿
3. **磁盘缓存 TTL**: 章节正文磁盘缓存有效期 7 天
4. **进度保存间隔**: 定时保存间隔 10 秒
5. **冷却机制**: 章节切换后 800ms 内不触发自动加载上一章
6. **防抖机制**: 加载上一章完成后 500ms 冷却时间
7. **主题切换恢复**: 主题切换后需要恢复滚动位置
8. **横竖屏切换恢复**: 视口尺寸变化后需要恢复滚动位置

---

## 九、重构优先级

### 高优先级（必须拆分）
1. `index.dart` 拆分（1343行 → 多个工具文件）
2. `logic.dart` 拆分（1329行 → 多个工具文件）
3. 数据模型提取到 `models/` 目录

### 中优先级（建议拆分）
1. 章节缓存逻辑独立
2. 进度计算逻辑独立
3. 滚动处理逻辑独立

### 低优先级（可选优化）
1. 子组件样式文件补全
2. 注释完善
3. 多语种适配检查

---

## 十、测试要点

1. 首次进入页面加载流程
2. 阅读进度恢复（有进度/无进度）
3. 章节无限滚动加载（上一章/下一章）
4. 目录跳转
5. 自动阅读功能
6. 主题切换（日间/夜间）
7. 横竖屏切换
8. 点赞/收藏/评论/分享
9. 下拉刷新
10. 错误状态处理
