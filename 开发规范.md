
1、所有变量、文件、文件夹、函数等命名规范统一用小写+下划线

2、能抽离子组件处理的内容一定抽离给子组件处理

3、能复用的代码尽可能复用

4、尽量不用魔法数字硬编码硬编码，进来全部用变量，放在style.dart文件内统一管理，子组件内也可以用自己的style.dart文件

5、index.dart文件负责处理UI, logic.dart文件负责处理逻辑交互，style.dart 负责处理文字、方块等交互，widgets 负责存放子组件代码

6、注释尽可能详细，写清楚每一行代码的注释，每一个函数写清楚作用，参数每一个的用途，每一个变量的用途都要写清楚

7、尽量每一个函数都单独设置一个文件去存放

8、目录结构：

    assets - 静态资源目录
        i18n - 多语种文件目录 
        img - 静态资源图片目录 
        svg - svg图标目录 
        font - 字体文件存放目录 

    lib - 源码目录
        api - 网络请求中间件
            post_request.dart - 所有网络请求统一通过这个文件进行处理
        components - 公共组件目录
        config - 公共配置文件
            color_config.dart - 所有颜色配置都在这里设置，如无特殊情况，红色指dangerColor变量的颜色，绿色指successColor变量的颜色，主题色指themeColor变量的颜色
        models - 网络请求解析模型
        pages - 路由页面
        stores - 全局数据保存
        util - 公用函数
        main.dart - 程序入口文件


9、所有UI的更改都要适配日间主题和夜间主题

10、多语种文字长度适配（CJK 语系 vs 非 CJK 语系）
    - 中文/日文/韩文（CJK）字符方正紧凑，单词宽度小；英语等拉丁字母单词宽度通常是中文的 2-3 倍
    - 所有涉及文字展示的 UI 组件，必须同时提供 CJK 和非 CJK 两套样式参数，放在 style.dart 中统一管理
    - 使用 LanguageUtil.is_cjk_language(languageCode) 判断当前语种，不要硬编码 ['zh', 'ja', 'ko'] 列表
    - 典型需要适配的场景：
      a. Tab 栏字号、间距、缩放比例（英文 Tab 更宽，需缩小字号和间距）
      b. 榜单子 Tab 的间距和字号
      c. 统计卡片的高度和标签字号（英文标签更长）
      d. 按钮最小宽度（英文按钮更宽）
      e. 行高（英文字母有升部降部，需要更大的行高保证可读性）
    - style.dart 中命名规范：xxx_cjk / xxx_alphabetic 后缀区分两套参数
    - 示例：
      static const double tab_font_size_cjk = 15;
      static const double tab_font_size_alphabetic = 13;

11、字体字重规范（Android/iOS 跨平台适配）
    - Android 默认思源黑体（Noto Sans SC）比 iOS 苹方（PingFang SC）视觉更粗，同一字重在 Android 上需要降一档才能与 iOS 视觉一致
    - 所有 FontWeight 必须用 FontConfig.adjustedWeight() 包裹，不要直接使用裸 FontWeight
    - 正文使用 FontConfig.adjustedWeight(FontWeight.w400)，标题/强调使用 FontConfig.adjustedWeight(FontWeight.w500)
    - 使用时需要引入：import 'package:app/config/font_config.dart';
    - 注意事项：
      a. static const FontWeight 必须改为 static final FontWeight（因为 adjustedWeight 是方法调用，不是编译期常量）
      b. 包含 adjustedWeight 的 TextStyle 不能使用 const 关键字（如 const TextStyle(...) 需改为 TextStyle(...)）
      c. 同理，包含 adjustedWeight 的 StrutStyle 也不能使用 const
    - 错误示例：
      static const FontWeight weight = FontConfig.adjustedWeight(FontWeight.w500);  // ✗ 编译错误
      style: const TextStyle(fontWeight: FontConfig.adjustedWeight(FontWeight.w400))  // ✗ 编译错误
    - 正确示例：
      static final FontWeight weight = FontConfig.adjustedWeight(FontWeight.w500);  // ✓
      style: TextStyle(fontWeight: FontConfig.adjustedWeight(FontWeight.w400))  // ✓

